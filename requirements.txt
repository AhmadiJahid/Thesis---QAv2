# Thesis QAv2 - pipeline dependencies.
#
# Versions are pinned to what the v1 working environment actually had installed
# (read from its .venv dist-info at port time, 2026-08-12) rather than guessed, so a
# fresh environment reproduces the versions the v1 results were produced under.
# Anything not determinable from that environment is left unpinned instead of invented.

# Core numerics / data
numpy==2.4.1
scipy==1.17.1
pandas==3.0.0
scikit-learn==1.8.0
# Imported directly by MusiQue/scripts/sample_pool.py (clustered pool construction,
# issue #14): threadpool_limits is what makes the k-means fit single-threaded and
# therefore bit-reproducible under the seed (ADR 0021 item 7). It arrives as a
# scikit-learn dependency anyway, but a determinism guarantee should not rest on a
# transitive pin. Version read from the installed .venv on 2026-08-20, not guessed.
threadpoolctl==3.6.0

# Graph edit distance for the Break-faithful GED metric
# (scripts/musique_decompositions_evaluator.py, issue #40): the reference graph of a
# decomposition and nx.optimize_graph_edit_distance over it. It already arrived here as a
# torch dependency, but a metric in the evaluation path should not rest on a transitive pin.
# Version read from the installed .venv on 2026-08-22, not guessed. (Break's own
# requirements.txt pins networkx==2.4; the two APIs used here - DiGraph and
# optimize_graph_edit_distance - exist in both.)
networkx==3.6.1

# Plotting (scripts/analyze_runs.py, plot_pool_sweep.py, plot_musique_chunk_stats.py,
# compare_answer_accuracy.py --report)
matplotlib==3.10.8
seaborn==0.13.2

# Models: routing / decomposition inference, retrieval, NER masking
torch==2.10.0
transformers==5.5.4
sentence-transformers==5.2.3
datasets==4.8.4
huggingface-hub==1.11.0

# device_map="auto" (router and decomposer loaders) needs accelerate;
# --quantization 4bit/8bit (decomposer) needs bitsandbytes.
accelerate==1.12.0
bitsandbytes==0.49.2

# LoRA / QLoRA fine-tuning of the decomposer (components/decomposer/train_lora.py, issue #13).
# These two versions are not guessed: `pip install --dry-run peft trl` was run against the
# v2 .venv on 2026-08-18 (transformers 5.5.4, torch 2.10.0, accelerate 1.12.0, datasets
# 4.8.4 already installed) and resolved to exactly these, reporting "Would install
# peft-0.20.0 trl-1.10.0" with no upgrade or downgrade of any pinned package above; the
# real install then changed nothing else. Additive only - if a future resolution wants to
# move one of the pins above, stop rather than let it.
peft==0.20.0
trl==1.10.0

# Entity masking (Aho-Corasick)
pyahocorasick==2.3.0
