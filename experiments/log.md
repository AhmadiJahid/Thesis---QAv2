<!-- Generated by Codery — setup session with Tural, 2026-08-11. Amend via issues in this repo. -->

# Experiment log

The single record of every experiment. **Append-only.** One row per experiment, ids `exp-001` onward.

## Rules

- **An entry is REQUIRED before a run starts** (hypothesis + config) and **completed after** (metrics). No experiment exists unless it is in this log.
- **No metric claim anywhere without a log entry** to cite.
- A **"this improved"** claim requires the *same evaluation set* as the baseline it is compared against.
- This log is how parallel sessions coordinate — read the last entries before starting any experiment work.

## Schema

| id | date | owner (human:session or agent) | hypothesis | approach/config | commit | seed | model | eval set | metrics | notes / learnings |
|---|---|---|---|---|---|---|---|---|---|---|

<!-- Illustrative row (delete this comment block when the first real entry lands):
| exp-001 | 2026-08-11 | jahid:session-1 | <one line: what you expect this run to show> | <approach>, configs/exp-001.yaml | abc1234 | 42 | 8B instruct (≤8B cap) | MuSiQue | EM — / ROUGE-F1 — / step P — R — | what the run showed, and anything the next reader needs to know |
-->

## Experiments

| id | date | owner (human:session or agent) | hypothesis | approach/config | commit | seed | model | eval set | metrics | notes / learnings |
|---|---|---|---|---|---|---|---|---|---|---|
| exp-001 | 2026-08-19 | experiment-runner:session-1 | LoRA fine-tuning on the full MuSiQue train split is a comparison arm against the prompting method; this run produces the trained adapter and its training-side record. | `configs/finetune_decomposer.json` arm `full_train` (full MuSiQue train split, no cap, no hop filter), via `components/decomposer/train_lora.py --arm full_train` | 2f49c20 | 42 | mistralai/Mistral-7B-Instruct-v0.3 (4-bit QLoRA, LoRA r=16) | MuSiQue ADR 0007 pinned 600 (200/hop) — evaluation deferred to a later run | training completed 2026-08-19 05:42 — train loss 0.140, 2 epochs / 2494 steps, 19,938 examples, wall clock 15,497.5 s, peak GPU 5.05 GiB; decomposition quality UNMEASURED (deferred to the eval run per design) | Training-only run (issue #13). Adapter at `runs/exp-001/20260818_222259/adapter`; committed artifacts in `experiments/exp-001/`. Run notes flag the working tree dirty by one untracked file (`Plan after the meeting.md`) at launch — code itself was at 2124333. Evaluation (quality + cost vs prompting baseline) happens in a separate later run via `scripts/compare_decomposer_arms.py`. `pool_2000` arm is BLOCKED on Jahid's "best pool" decision (issue #13, 2026-08-19 comment); `generalisation_2_3hop` queued to run after this one. |
| exp-002 | 2026-08-19 | experiment-runner:session-1 | If the oracle hop count materially improves decomposition quality over unguided, a router is justified. If unguided_capped closes most of that gap, the problem was runaway generation length rather than hop ignorance. | `configs/decomposer_musique.json`, all three conditions (`unguided`, `oracle_guided`, `unguided_capped`) via `components/decomposer/run_decomposer.py --model mistral_7b_instruct --config decomposer_musique.json --condition <cond>` | b6329ca (pipeline code 1ccea11) | 42 | mistralai/Mistral-7B-Instruct-v0.3 (issue #12) | MuSiQue ADR 0007 pinned 600 (200/hop), retrieval artifact per ADR 0014 (v1 9,156-example pool, sha256 e5c418a9b25f...) | FAILED — all 3 arms rc=1 at launch (2026-08-19 13:37:56), `KeyError: 'raw'` at `run_decomposer.py:1899` (per-query dump read `gen["raw"]`; `generate()` returns `"text"`). Zero decompositions produced; unmeasured. | Root cause: a dry run has `gen = None`, so preflight (dry-run) could not reach the crashing branch — only real generation hit it. Combined log: `runs/exp-002-003-combined.log`. Fixed in `bfbef15` (PR #26, Gate-1 reviewed, AST contract test added); main now at `8bcf70b`. Relaunched as `exp-004` (this model) per the fix. |
| exp-003 | 2026-08-19 | experiment-runner:session-1 | If the oracle hop count materially improves decomposition quality over unguided, a router is justified. If unguided_capped closes most of that gap, the problem was runaway generation length rather than hop ignorance. | `configs/decomposer_musique.json`, all three conditions (`unguided`, `oracle_guided`, `unguided_capped`) via `components/decomposer/run_decomposer.py --model qwen3_5_9b --config decomposer_musique.json --condition <cond>` | b6329ca (pipeline code 1ccea11) | 42 | Qwen/Qwen3.5-9B (admitted above the ~8B ceiling by ADR 0015, pending supervisor confirmation; issue #12) | MuSiQue ADR 0007 pinned 600 (200/hop), retrieval artifact per ADR 0014 (v1 9,156-example pool, sha256 e5c418a9b25f...) | FAILED — all 3 arms rc=1 at launch (2026-08-19 13:37:56), `KeyError: 'raw'` at `run_decomposer.py:1899` (per-query dump read `gen["raw"]`; `generate()` returns `"text"`). Zero decompositions produced; unmeasured. | Root cause: a dry run has `gen = None`, so preflight (dry-run) could not reach the crashing branch — only real generation hit it. Combined log: `runs/exp-002-003-combined.log`. Fixed in `bfbef15` (PR #26, Gate-1 reviewed, AST contract test added); main now at `8bcf70b`. Relaunched as `exp-005` (this model) per the fix. Qwen arms are out-of-scope-pending if the supervisor reasserts the 8B ceiling (ADR 0015). |
| exp-004 | 2026-08-19 | experiment-runner:session-1 | If the oracle hop count materially improves decomposition quality over unguided, a router is justified. If unguided_capped closes most of that gap, the problem was runaway generation length rather than hop ignorance. | `configs/decomposer_musique.json`, all three conditions (`unguided`, `oracle_guided`, `unguided_capped`) via `components/decomposer/run_decomposer.py --model mistral_7b_instruct --config decomposer_musique.json --condition <cond>` | 8bcf70b (pipeline code bfbef15, fix for #26; also carries #25 adapter guards) | 42 | mistralai/Mistral-7B-Instruct-v0.3 (issue #12) | MuSiQue ADR 0007 pinned 600 (200/hop), retrieval artifact per ADR 0014 (v1 9,156-example pool, sha256 e5c418a9b25f...) | COMPLETE — all 3 arms rc=0, 600/600 rows, 0 empty decompositions. Overall composite/step_f1/ordered_step_acc/hop_count_EM: unguided 0.2098/0.2039/0.1804/0.5083; oracle_guided 0.4197/0.2061/0.1852/0.5900; unguided_capped 0.2128/0.2040/0.1805/0.5083 (step-line cap fired on 12/600 rows, 7 actually cut off). Per-hop (over/under/step_count_MAE/mean_signed_error) hop=2/3/4: unguided 0.280/0.010/0.450/+0.430, 0.290/0.195/0.705/+0.315, 0.300/0.400/1.170/+0.260; oracle_guided 0.150/0.000/0.170/+0.170, 0.370/0.065/0.560/+0.430, 0.525/0.120/0.920/+0.680; unguided_capped 0.280/0.010/0.420/+0.400, 0.290/0.195/0.665/+0.275, 0.300/0.400/0.975/+0.065. --compare (n=600, bootstrap 95% CI + McNemar + paired t-test): unguided vs oracle_guided — step_f1 diff -0.0022 CI[-0.0129,+0.0085] not sig (t p=0.686); ordered_step_acc -0.0048 CI[-0.0155,+0.0061] not sig (t p=0.378); composite -0.2098 CI[-0.2189,-0.0058] sig; hop_count_EM -0.0817 McNemar p=0.000241 sig, t p=0.000182 sig; exact_match +0.0050 McNemar p=0.581 not sig. unguided vs unguided_capped: all quality metrics tie (composite -0.0030 CI[-0.0072,-0.0004] sig, driven by step-count-error term only; exact_match/hop_count_EM identical, 0 discordant pairs). oracle_guided vs unguided_capped mirrors unguided-vs-oracle in sign, oracle ahead. Cost/query (prompt+completion tok, latency s): unguided 420.4+79.3=499.8tok/1.879s; oracle_guided 475.4+120.5=595.9tok/2.844s; unguided_capped 420.4+77.4=497.9tok/1.857s. Full per-hop + comparison JSONs: `experiments/exp-004/metrics.json`. | Relaunch of exp-002 at the fixed SHA (`gen["raw"]` -> `gen["text"]`, PR #26). Dry-run preflight re-run at this SHA for all 3 conditions before launch: 600/600 pinned ids, 200/200/200 per hop, retrieval sha256 matches ADR 0014. GPU contended at launch time by another user's ollama process (~21.2 GiB of 24 GiB); launch waits for headroom per docs/compute.md. Generation completed overnight 2026-08-21/22 via the rearmed day-2 waiter; evaluated at commit 0068842 (no pipeline code changed since generation at 6fc4bba). Reading: on Mistral-7B, oracle guidance moves hop-count-EM and composite significantly but NOT step_f1/ordered_step_accuracy (CIs straddle zero, t-tests not significant) — helps get the step *count* right more often at +19% tokens/+51% latency, without a measured step-quality gain. unguided_capped is statistically indistinguishable from unguided (cap fired on only 12/600 rows) — this run cannot separate "runaway length" from "hop ignorance" as the dominant failure mode. No router-design recommendation made; that decision is Jahid's/the supervisor's. Committed artifacts: `experiments/exp-004/{config.json,metrics.json,notes.md}`. |
| exp-005 | 2026-08-19 | experiment-runner:session-1 | If the oracle hop count materially improves decomposition quality over unguided, a router is justified. If unguided_capped closes most of that gap, the problem was runaway generation length rather than hop ignorance. | `configs/decomposer_musique.json`, all three conditions (`unguided`, `oracle_guided`, `unguided_capped`) via `components/decomposer/run_decomposer.py --model qwen3_5_9b --config decomposer_musique.json --condition <cond>` | 8bcf70b (pipeline code bfbef15, fix for #26; also carries #25 adapter guards) | 42 | Qwen/Qwen3.5-9B (admitted above the ~8B ceiling by ADR 0015, pending supervisor confirmation; issue #12) | MuSiQue ADR 0007 pinned 600 (200/hop), retrieval artifact per ADR 0014 (v1 9,156-example pool, sha256 e5c418a9b25f...) | COMPLETE — all 3 arms rc=0, 600/600 rows, 0 empty decompositions. Overall composite/step_f1/ordered_step_acc/hop_count_EM: unguided 0.4212/0.2131/0.1911/0.5200; oracle_guided 0.4429/0.2368/0.2205/0.8733; unguided_capped 0.4215/0.2131/0.1911/0.5200 (step-line cap fired on 4/600 rows, 2 actually cut off). Per-hop (over/under/step_count_MAE/mean_signed_error) hop=2/3/4: unguided 0.280/0.005/0.335/+0.325, 0.400/0.130/0.695/+0.435, 0.415/0.210/0.895/+0.435; oracle_guided 0.025/0.000/0.025/+0.025, 0.105/0.060/0.745/+0.625, 0.150/0.040/0.845/+0.765; unguided_capped identical to unguided at hop 2/3, hop=4 0.415/0.210/0.870/+0.410. --compare (n=600, bootstrap 95% CI + McNemar + paired t-test): unguided vs oracle_guided — step_f1 diff -0.0237 CI[-0.0351,-0.0123] sig (t p=4.8e-05); ordered_step_acc -0.0293 CI[-0.0403,-0.0188] sig (t p=1.3e-07); composite -0.0217 CI[-0.0348,-0.0070] sig; hop_count_EM -0.3533 McNemar p=5.0e-48 sig, t p=1.0e-48 sig; exact_match -0.0117 McNemar p=0.0391 sig, t p=0.0195 sig — unlike Mistral, EVERY quality metric moves significantly in oracle_guided's favour on this model. unguided vs unguided_capped: statistically indistinguishable on every metric (cap fired on only 4/600 rows). oracle_guided vs unguided_capped mirrors unguided-vs-oracle in sign and magnitude, oracle ahead on every metric. Cost/query (prompt+completion tok, latency s): unguided 398.4+34.8=433.2tok/2.479s; oracle_guided 446.4+44.2=490.5tok/3.129s; unguided_capped 398.4+34.8=433.1tok/2.474s. Full per-hop + comparison JSONs: `experiments/exp-005/metrics.json`. | Relaunch of exp-003 at the fixed SHA (`gen["raw"]` -> `gen["text"]`, PR #26). Dry-run preflight re-run at this SHA for all 3 conditions before launch: 600/600 pinned ids, 200/200/200 per hop, retrieval sha256 matches ADR 0014. GPU contended at launch time by another user's ollama process (~21.2 GiB of 24 GiB); launch waits for headroom per docs/compute.md. Qwen arms are out-of-scope-pending if the supervisor reasserts the 8B ceiling (ADR 0015). Generation completed overnight 2026-08-21/22 via the rearmed day-2 waiter; evaluated at commit 0068842 (no pipeline code changed since generation at 6fc4bba). Reading: on Qwen3.5-9B, oracle guidance materially and significantly improves decomposition quality across the board (step_f1, ordered_step_accuracy, rouge_l_f1, exact_match, hop_count_EM all p<0.05 by bootstrap CI, McNemar, and paired t-test) at +13% tokens/+26% latency — a broader signal than the same comparison on Mistral-7B (exp-004). unguided_capped is statistically indistinguishable from unguided here too (cap fired on only 4/600 rows). No router-design recommendation made; that decision is Jahid's/the supervisor's. Committed artifacts: `experiments/exp-005/{config.json,metrics.json,notes.md}`. |
| exp-006 | 2026-08-20 | experiment-runner:session-1 | Issue #14 / ADR 0021: the clustering pool-construction strategy at size 2000 should end up sitting next to the existing `imbalanced` and `balanced` strategies at the same size, same dev sample, same retrieval variants — so the sweep's decomposition metrics are a construction-vs-construction comparison, not a confound of size or eval set. Verified in this log before starting: no v2 `pool_sweep` cell (any of the three strategies) has ever produced metrics — the sweep has only ever dry-run in v2 (PR #34/#35/#36 added the clustered strategy, code and significance tooling, but no `pool_sweep` run landed here). Minimal set to answer #14: `(size=2000, balance) x (variant, mode)` for `balance in {imbalanced, balanced, clustered}`, `variant in {biencoder_only, biencoder_plus_ce}`, `mode in {raw, typed, uniform}` — 3 x 2 x 3 = 18 decompose+eval cells, one trial each (`pool_trial_seeds: [42]`), all against one dev sample (`dev_seed 42`, `dev_per_hop 250`, 3 coarse buckets = 750 queries). This run covers only the CPU-feasible half of that plan (pool construction + bi-encoder similarity + cross-encoder rerank, all 3 strategies); the GPU half (mistral_7b_instruct decomposer generation x18 + eval) is queued behind exp-004/exp-005's hold on `runs/run.lock`. | `configs/pool_sweep.json` (`clustered_sizes: [2000]`, `imbalanced_sizes`/`balanced_sizes` include 2000; `retrieval_modes`, `retriever_variants`, `dev_seed`, `dev_per_hop`, `base_pool_seed`/`pool_trial_seeds`), `configs/musique_prep.json` `sample_pool.clustering` (ADR 0021 knobs), `configs/similarity.json`. Ran `MusiQue/scripts/sample_dev.py`, `sample_pool.py` (x3 balances), `check_question_similarity.py`, `truncate_top20.py`, `rerank_similarity_results.py` directly (not through `scripts/pool_sweep_orchestrator.py`) with every value the orchestrator would pass left at the child script's own committed default (`--config musique_prep.json` / `similarity.json`, `--embed-model e5-small`, `--top-k 20`, `--k 5`/`--rerank-k 5`, `--cross-encoder cross-encoder/ms-marco-MiniLM-L-12-v2`) and `--device cpu` explicit — this is a deliberate deviation from `pool_sweep.json`'s own top-level `"device": "cuda"`, which the orchestrator forces onto these same stages unconditionally (confirmed live via `--dry-run`: it prints `--device cuda` for `similarity`/`rerank` on every cell and for `sample_pool` on the clustered cell). CPU is not an invented value: `configs/similarity.json`'s own `"device"` key already defaults to `"cpu"`, and ADR 0021 item 7 records that this repo's only *verified* bit-reproducibility check for the clustered strategy's k-means fit is CPU-only (GPU embedding determinism is explicitly unverified there) — so CPU is the more, not less, defensible device for this precompute. Every output was written to the exact path `pool_sweep_orchestrator.py`'s own `_pool_dir`/`_sim_dir`/`_bi_top5_dir`/`_ce_dir`/`_dev_sample_path` helpers produce (verified against the orchestrator's own `--dry-run` printout for `--only size2000_imbalanced,size2000_balanced,size2000_clustered`), so a later `--stage decompose` (or `--stage all`) invocation skips this precompute via its existing skip-if-exists logic and proceeds straight to the GPU stage. | b0a9ce8 | 42 | none in this run (embed `intfloat/e5-small-v2` via `e5-small` alias; cross-encoder `cross-encoder/ms-marco-MiniLM-L-12-v2`; decomposer `mistralai/Mistral-7B-Instruct-v0.3` (`mistral_7b_instruct`) queued for the deferred GPU stage) | pool_sweep's own dev sample (`dev_seed 42`, `dev_per_hop 250`, coarse buckets `2hop/3hop/4hop` = 750 queries) — distinct from ADR 0007's pinned 600 (200/hop) used by exp-002..005; decomposition-quality metrics are still PENDING, deferred to the GPU follow-up | CPU precompute COMPLETE (all 3 strategies, rc=0 every stage, ~13.7 min wall clock 21:09:17→21:23:00 local): pool.jsonl 2000/2000/2000 rows (hop split imbalanced 1461/413/126, balanced 667/667/666, clustered 1396/495/109); top20/top5-biencoder/top5-ce all 750/750/750 rows per strategy. Clustered k-means: 19,938 candidates→k=2000, 11 iters, inertia 1803.46, 0 empty clusters, 0 rows from top-up, embed 33.4s + fit 25.4s (CPU, single-threaded per ADR 0021). Decomposer generation + eval (18 cells: 3 balances x 2 variants x 3 modes) still NOT RUN — queued behind exp-004/exp-005's `runs/run.lock` hold. Full metrics: `experiments/exp-006/metrics.json`. | Ran detached in tmux session `exp-006` (session ended on completion), log `runs/exp-006/cpu_stages.log`, launcher `runs/exp-006/run_cpu_stages.sh`. Never touched `runs/run.lock`, the exp-004/005 waiter, or its tmux session — confirmed by `nvidia-smi` identical before and after (21279 MiB used / 2972 MiB free both times). Committed artifacts: `experiments/exp-006/{config.json,metrics.json,notes.md}`; precompute outputs stay on the box under `runs/pool_sweep/{dev_sample,pools,similarity,biencoder_top5,rerank}/`. Flag for the GPU follow-up: this dev sample carries no `question_masked_typed`/`question_masked_uniform` fields (`sample_dev.py` reported `typed=False, uniform=False`); whether that degrades the `typed`/`uniform` retrieval modes was not checked here. Follow-up command once the lock frees: `pool_sweep_orchestrator.py --stage decompose --only size2000_imbalanced,size2000_balanced,size2000_clustered` then `--stage eval` (or one `--stage all` pass) — it will skip this run's precompute via skip-if-exists and go straight to the GPU stage. |
| exp-007 | 2026-08-21 | experiment-runner:session-1 | Issue #15 / ADR 0022: whether hop-matched few-shot retrieval helps decomposition quality is unmeasured. This run produces the CPU-side retrieval artifacts for two of the three conditions (mixed, oracle-hop-matched) on the same eval set and same pool, so a later GPU decomposition + eval pass turns the gap into a number instead of an assumption. Decomposition-quality metrics are explicitly deferred; this entry covers artifact production only. The router-hop-matched condition is NOT runnable (issue #15 comment, ADR 0022 consequences: today's router writes no query id) and is not attempted here. | `MusiQue/scripts/check_question_similarity.py` -> `truncate_top20.py` -> `rerank_similarity_results.py`, run directly (not via an orchestrator) for `mixed` (`--no-hop-match`) and `oracle` (`--hop-match --hop-source gold`), both `--mode typed` (ADR 0006 item 4 default; masking default itself remains supervisor-open per ADR 0018 — #15's conditions are stated at the typed default, not a resolution of that question), `--top-k 20`, rerank `--rerank-k 5`, truncate `--k 5`, `--device cpu`, `--seed 42`, `configs/similarity.json` defaults otherwise (`e5-small` bi-encoder, `cross-encoder/ms-marco-MiniLM-L-12-v2` reranker). Queries: `--query-glob musique/dev_data/musique_ans_v1.0_dev_sample_*_hop_200.jsonl --n 200` = the ADR 0007 pinned 600 (200/hop, verified by dry-run preflight below). Pool: `--pool-file` pointed at the concrete file located and hash-verified against ADR 0014, `musique/chunks_only_question_masked_fixed/roberta_large_ner_english/musique_ans_v1.0_train_0_questions_all.jsonl` under the data root. The two conditions differ only in the hop_match flag; every other flag is identical. | d81dd5b | 42 | none (retrieval only: `intfloat/e5-small-v2` bi-encoder, `cross-encoder/ms-marco-MiniLM-L-12-v2` reranker; decomposer deferred to the GPU follow-up) | MuSiQue ADR 0007 pinned 600 (200/hop) | COMPLETE — CPU precompute only, both conditions rc=0. Row counts (re-verified independently at completion, not just from the per-stage trail): `mixed` and `oracle` each 600/600 at similarity_top20/truncate_top5/rerank_top5; hop split by query_id prefix on both final rerank_top5 files = 200/200/200. `oracle`'s hop_match provenance: hop_source=gold, min_candidates_resolved=20, per-hop pool candidates 3594/4387/1175 (hop 2/3/4) — every bucket clears min_candidates=top_k=20. `mixed`'s hop_match provenance: enabled=false on every query file (byte-identical code path, ADR 0022 item 2). Final artifact sha256 for all 6 files recorded in `experiments/exp-007/metrics.json`. Decomposition-quality metrics remain UNMEASURED here by design — deferred to exp-009. | Preflight (`--dry-run`, CPU, no encoder loaded) run for both conditions before launch: 600/600 pinned query ids loaded across the 3 glob-matched files, 200/200/200 per hop. Pool file located by matching its sha256 against ADR 0014's pinned hash: `sha256sum` of `musique_ans_v1.0_train_0_questions_all.jsonl` = `212c27634291b27ef55cebdb5feaba08a32a59f21976dd72a96a8f9f1ae66b2a`, exact match; 9,156 rows; observed hop split via query id prefix = 2-hop 3,594 / 3-hop 4,387 / 4-hop 1,175 — matches ADR 0022's cited bucket counts exactly. Oracle dry-run's per-bucket candidate counts (3594/4387/1175, all >= the default `min_candidates = top_k = 20`) confirm every hop bucket is feasible at top-k 20 before spending any CPU time on embeddings. GPU untouched: `runs/run.lock` (held `exp-004,exp-005` since 2026-08-19T15:03:03+03:00) and the exp-004/005 waiter were not touched; `nvidia-smi` shows 21279 MiB used / 2972 MiB free both before and after, attributable to another user's `ollama` process (pid confirmed via `nvidia-smi --query-compute-apps`), same as exp-006's note. Working tree carries one untracked non-pipeline file (`Plan after the meeting.md`, present at session start, not created by this run) — flagged by the run's own dirty-tree warning; does not affect any config or code path. **Completion housekeeping (2026-08-22, experiment-runner:session-1, no new run):** this row's metrics cell was left reading "PENDING — run launching" after the 6fc4bba commit even though the artifacts (`runs/exp-007/{mixed,oracle}/`, timestamps 01:02–01:05) finished minutes later; `experiments/exp-007/{config.json,metrics.json,notes.md}` were never committed (empty dir). Both are filled in now from the already-completed on-disk artifacts and their per-stage trail (`runs/exp-007/**/{similarity,truncate,rerank}/*_metrics.json`), re-verified independently (row counts, hop split, sha256) rather than copied blind. This is the required "completed after" edit to an existing row, not a new experiment; the GPU decomposition + eval follow-up is logged separately as exp-009. |
| exp-008 | 2026-08-22 | experiment-runner:session-1 | Issue #13: whether LoRA fine-tuning on the full MuSiQue train split beats the few-shot dynamic-prompting method on the same pinned eval set, and at what per-query cost, is unmeasured. This run produces the fine-tuned arm's decompositions and metrics on the ADR 0007 pinned 600 (inference + eval only; no training — `full_train`'s adapter is already trained, exp-001), so the comparison against exp-004's Mistral prompting arms becomes numbers. | `components/decomposer/run_decomposer.py --model mistral_7b_instruct --config decomposer_musique.json --condition unguided --adapter runs/exp-001/20260818_222259/adapter --no-few-shot --output-root runs/exp-008` (condition `unguided`'s prompt — guided false, no hop line, no few-shot block — matches the `full_train` training record exactly per `check_adapter_prompt_parity`, which the dry run below reports 0 mismatches on); eval via `scripts/musique_decompositions_evaluator.py`, then `scripts/compare_decomposer_arms.py --eval-arm full_train` against exp-004's mistral prompting arms (`configs/finetune_decomposer.json` arm `full_train`, `configs/musique_eval.json`) | ad252b4 | 42 | mistralai/Mistral-7B-Instruct-v0.3 + LoRA adapter (QLoRA r=16, trained exp-001 `full_train`, 2026-08-19; base+adapter within the 10B ceiling per ADR 0015) | MuSiQue ADR 0007 pinned 600 (200/hop), retrieval artifact per ADR 0014 (v1 9,156-example pool, sha256 e5c418a9b25f...; retrieval file supplies the query set even though `--no-few-shot` empties the example block) | COMPLETE — rc=0, 600/600 rows, 0 rows at max_new_tokens. Overall: composite 0.5236, step_f1 0.3411, ordered_step_acc 0.3237, hop_count_EM 0.7100, exact_match 0.1083, step_count_MAE 0.300, mean_signed_error -0.233 (under-decomposes, unlike all 3 prompting arms which over-decompose), over_rate 0.033, under_rate 0.257. Per-hop (step_f1/hop_count_EM/over/under/MAE/signed) hop=2: 0.4810/0.9750/0.020/0.005/0.025/+0.015; hop=3: 0.2930/0.7450/0.040/0.215/0.255/-0.175; hop=4: 0.2494/0.4100/0.040/0.550/0.620/-0.540. Cost/query: 122.5 prompt + 29.4 completion = 151.9 tok, 1.387s latency (vs unguided baseline's 499.8tok/1.879s — ~30% of tokens, ~74% of latency). Phase C `compare_decomposer_arms.py --eval-arm full_train --baseline unguided` (n=600 aligned, 200/200/200 per hop enforced on all 4 arms): full_train vs unguided — step_f1 +0.1372 CI[+0.1150,+0.1601] sig (t p=4.0e-29); ordered_step_acc +0.1433 CI[+0.1205,+0.1666] sig (t p=2.4e-31); rouge_l_f1 +0.1218 CI[+0.1065,+0.1369] sig (t p=7.7e-48); composite +0.3137 CI[+0.1055,+0.3307] sig; exact_match McNemar +0.0467 p=1.75e-04 sig (t p=1.29e-04); hop_count_EM McNemar +0.2017 p=1.12e-15 sig (t p=5.5e-16) — every quality metric significant in the fine-tuned arm's favour over the deployable prompting baseline, at lower cost. oracle_guided vs unguided and unguided_capped vs unguided reproduce exp-004's within-model numbers (included for context, not re-derived). full_train also exceeds oracle_guided on step_f1 (0.3411 vs 0.2061) and ordered_step_acc (0.3237 vs 0.1852) even though oracle_guided is not a deployable arm. Full tables: `experiments/exp-008/metrics.json`. | Dry-run preflight before launch: 600/600 pinned ids, 200/200/200 per hop, retrieval sha256 matches ADR 0014, `adapter_prompt_parity.mismatches == []` against the training record at `runs/exp-001/20260818_222259/config.json` (trained_on guided=false/prompt_unguided.md/plain/few_shot_examples_in_prompt=false; run renders the same), `assert_adapter_base_model` passes (both `mistralai/Mistral-7B-Instruct-v0.3`). `pool_2000` arm remains BLOCKED on Jahid's best-pool decision (issue #13); `generalisation_2_3hop` not trained; neither is touched here. `runs/run.lock` transferred from `exp-004,exp-005` (their GPU work is done; Phase A's evaluation was CPU-only) to `exp-008` for this run, then released after this entry and its artifacts were committed. Launched in tmux (`runs/exp-008/run.log`), GPU free at launch and throughout (no contention this time). Baseline for Phase C is `unguided` (the deployable dynamic-prompting condition) per the brief; `configs/finetune_decomposer.json` does not designate a different baseline protocol. `pool_2000`/`generalisation_2_3hop` remain unmeasured — this entry covers `full_train` only. Committed artifacts: `experiments/exp-008/{config.json,metrics.json,notes.md}`. |
| exp-009 | 2026-08-22 | experiment-runner:session-1 | Issue #15 / ADR 0022 GPU follow-up: whether hop-matched few-shot retrieval (oracle-hop-matched) improves decomposition quality over mixed retrieval, at the `unguided` (deployable) prompting condition, on the ADR 0007 pinned 600. Router-hop-matched is out of scope (no query id from today's router, ADR 0022 item 5/Consequences) — not attempted, not stubbed, not estimated. | `components/decomposer/run_decomposer.py --model mistral_7b_instruct --config decomposer_musique.json --condition unguided --retrieval-input runs/exp-007/<mixed\|oracle>/<cond>_rerank_top5.jsonl --output-root runs/exp-009/<mixed\|oracle>`, everything else (seed, model, decoding, `--retrieval-mode`/`--retrieval-k` left at config defaults `typed`/5, matching the ADR 0006 masking default the exp-007 artifacts were built at) identical between the two arms; retrieval-source wiring per PR #39 (d81dd5b) — `--retrieval-input` overrides `retrieval.input`/`retrieval.input_key` with no other code path touched (frozen launch-path files unchanged by PR #39). Eval via `scripts/musique_decompositions_evaluator.py` (per-hop reporting incl. over/under-decomposition rates) then `--compare mixed vs oracle_hop_matched` (bootstrap CIs + McNemar + paired t-test). | 133c45f | 42 | mistralai/Mistral-7B-Instruct-v0.3 (issue #12) | MuSiQue ADR 0007 pinned 600 (200/hop), retrieval artifacts `runs/exp-007/mixed/mixed_rerank_top5.jsonl` and `runs/exp-007/oracle/oracle_rerank_top5.jsonl` (exp-007, sha256 recorded in `experiments/exp-007/metrics.json`) | COMPLETE — both arms rc=0, 600/600 rows, 0 empty decompositions (mixed 23/600 rows at max_new_tokens, oracle 32/600). NOTE (issue #40, `docs/analysis/2026-08-22-finetuned-vs-prompting-error-analysis.md`, landed mid-run at `ec2f844`, docs-only): composite_score's 0.2-weight reference_validity term is decided by a regex/bare-vs-bracketed-`#k` mismatch — recorded per schema but NOT headlined; headline is step_f1 / ordered_step_accuracy / hop_count_exact_match. Overall (mixed / oracle_hop_matched): step_f1_macro 0.2035 / 0.1931; ordered_step_accuracy_macro 0.1804 / 0.1799; hop_count_exact_match_rate 0.5150 / 0.6000; exact_match_rate 0.0617 / 0.0567; composite_score 0.2099 / 0.2633 (suspect, see note); reference_validity_micro 0.0 / 0.2778; over/under-decomposition 0.285/0.200 vs 0.270/0.130; step_count_MAE 0.77 / 0.703. Per-hop (step_f1/ordered_step_acc/hop_count_EM) hop=2/3/4: mixed 0.327/0.318/0.710, 0.168/0.138/0.525, 0.116/0.085/0.310; oracle 0.355/0.345/0.845, 0.153/0.135/0.580, 0.071/0.060/0.375 — oracle's hop-count gain concentrates at hop 2 and 3/4 hold smaller gains, but oracle's step_f1/ordered_step_acc are behind mixed at hop 4 (the smallest pool bucket, 1,175/9,156 rows — ADR 0022's stated confound). --compare (n=600, bootstrap 10000 resamples seed 42 + McNemar + paired t-test): step_f1 diff +0.0104 CI[-0.0064,+0.0273] not sig (t p=0.226); ordered_step_accuracy +0.0005 CI[-0.0157,+0.0168] not sig (t p=0.950); rouge_l_f1 -0.0038 CI[-0.0159,+0.0079] not sig (t p=0.530); composite_score -0.0534 CI[-0.1974,+0.1856] not sig by bootstrap (suspect metric regardless); exact_match McNemar p=0.664 not sig (t p=0.513); hop_count_exact_match McNemar p=0.0006164 (b=82,c=133) SIG, t p=0.00048 SIG (mixed 0.515 vs oracle 0.600, oracle ahead). Reading: hop-matched retrieval moves whether the model gets the step COUNT right significantly, with no measured step-QUALITY gain (step_f1/ordered_step_accuracy both not significant) — the same shape as exp-004's guided/unguided finding, on an unrelated axis (few-shot retrieval vs stated hop count). Full tables: `experiments/exp-009/metrics.json`. Router-hop-matched remains unmeasurable; the oracle-minus-router error-propagation number is NOT estimated anywhere in this entry. | Preflight before launch (both conditions, `--dry-run`): confirmed 600/600 pinned ids load from each retrieval file and 200/200/200 per hop, 0 ids_missing/ids_unexpected, retrieval input sha256 matched exp-007's artifacts exactly. `runs/run.lock` acquired for `exp-009`; GPU verified idle at acquisition (`nvidia-smi`: 10 MiB used / 24242 MiB free, no compute processes). Launched detached in tmux (`runs/exp-009/run_exp009.sh`, log `runs/exp-009/run.log`); completed with tmux session ended, `overall_rc=0`. Generation ran from the process image loaded at commit `133c45f`; `main` advanced to `ec2f844` (docs-only, issue #40's analysis note, verified to touch no pipeline code) while the GPU run was in flight, so the runner's own trail records `git.commit: ec2f844` (it reads HEAD at write time) even though no code relevant to this run changed between the two commits — see `experiments/exp-009/config.json`'s `note_on_commit_delta`. `runs/run.lock` released after this entry and its artifacts were committed. Committed artifacts: `experiments/exp-009/{config.json,metrics.json,notes.md}`. |
| exp-011 | 2026-08-22 | experiment-runner:session-1 | Issue #40 / PR #44 follow-up: PR #44 (merged `a99b573`) added additive Break-faithful EM/SARI/GED + repaired chain validity columns to `scripts/musique_decompositions_evaluator.py`. exp-010's 18 cells were evaluated at/after `a99b573` and already carry the new columns; nine older committed arms (exp-004 x3, exp-005 x3, exp-008 x1, exp-009 x2) were evaluated at earlier commits (`0068842`, `374f862`, `ec2f844`, all pre-`a99b573`) and lack them. This is a coverage re-score, not a re-comparison: re-run the evaluator over each arm's existing `results.json` predictions (same predictions, same gold, same eval set as the original row) at current `main` so the new columns exist for all nine, with no pipeline code or config changed. | `scripts/musique_decompositions_evaluator.py --config musique_eval.json --predictions <arm results.json> --run-dir runs/exp-011/<arm> --out-prefix eval --seed 42` (config/gold/composite weights unedited, `configs/musique_eval.json`), once per arm: exp004_unguided (`runs/exp-004/20260821_222907/results.json`), exp004_oracle_guided (`.../20260821_224805/results.json`), exp004_unguided_capped (`.../20260821_231641/results.json`), exp005_unguided (`runs/exp-005/20260821_233524/results.json`), exp005_oracle_guided (`.../20260822_000025/results.json`), exp005_unguided_capped (`.../20260822_003156/results.json`), exp008_full_train (`runs/exp-008/20260822_100745/results.json`), exp009_mixed (`runs/exp-009/mixed/20260822_103146/results.json`), exp009_oracle_hop_matched (`runs/exp-009/oracle/20260822_105045/results.json`). No `--compare` (no cross-arm claims here; those already exist in exp-004/005/008/009's own rows on their own eval sets). CPU-only, no GPU touched. | f4a1a19e68a518f41857f3de8cc8904650f11d04 | 42 | none (re-scoring existing predictions; original arms' models: mistralai/Mistral-7B-Instruct-v0.3 base/LoRA per exp-004/008/009, Qwen/Qwen3.5-9B per exp-005) | Same as each arm's original row: MuSiQue ADR 0007 pinned 600 (200/hop) for all 9 arms (verified per-arm results.json row count = 600 before launch) | COMPLETE — 9 of 9 arms re-scored, all rc=0, 600/600 rows evaluated, 0 missing gold on every arm; every re-scored arm reproduces its original `step_f1_macro`/`ordered_step_accuracy_macro`/`hop_count_exact_match_rate`/`composite_score` exactly (additive-only confirmed empirically, not just by docstring). New columns (`break_exact_match_rate` / `sari_macro` / `ged_macro`, lower better / `ged_fallback_counts.node_cap` / `chain_validity_macro`), composite_score recorded but NOT headlined (issue #40 reference_validity defect, unrelated to these columns): exp004_unguided 0.0533/0.5850/0.4715/1/0.9686 (composite 0.2098); exp004_oracle_guided 0.0500/0.5890/0.4414/0/0.9712 (0.4197); exp004_unguided_capped 0.0533/0.5849/0.4708/0/0.9686 (0.2128); exp005_unguided 0.0517/0.5979/0.4910/0/0.8605 (0.4212); exp005_oracle_guided 0.0600/0.6033/0.4241/6/0.8697 (0.4429); exp005_unguided_capped 0.0517/0.5978/0.4909/0/0.8605 (0.4215); exp008_full_train 0.1017/0.6823/0.3094/0/0.9942 (0.5236); exp009_mixed 0.0533/0.5851/0.4713/1/0.9686 (0.2099); exp009_oracle_hop_matched 0.0483/0.5879/0.4540/1/0.9686 (0.2633). Descriptive reading (no --compare run, no significance testing, no new cross-arm claims): exp008_full_train (LoRA fine-tuned Mistral-7B) leads every new metric by a wide margin, consistent with its already-logged significant advantage on the pre-existing metrics. chain_validity_macro separates by base model rather than by prompting condition: all three exp-005 (Qwen3.5-9B) arms sit at 0.860-0.870, visibly below every Mistral-7B arm (0.969-0.994) — notable because exp-005's own row has Qwen ahead of Mistral on step_f1/ordered_step_accuracy, so this axis does not obviously track that ranking; flagged as an observation, not a tested comparison. Within each triple, oracle_guided leads unguided/unguided_capped on all four new metrics in both exp-004 and exp-005, same direction as those arms' already-logged finding; unguided and unguided_capped are near-identical on every new metric in both triples. GED fallback (node_cap, deterministic per config docstring) fired on at most 6/600 rows in any arm. Full per-arm tables + per-hop breakdown + provenance (predictions sha256, run dirs): `experiments/exp-011/metrics.json`. | Preflight before launch: all 9 arms' `results.json` located and readable (600/600 rows each, independently counted), all 9 arms' existing `eval_*_metrics.json` confirmed to predate `a99b573` (commits `0068842`/`374f862`/`ec2f844`, none carry `sari`/`ged`/`chain_validity`/`break_exact_match` keys) so this re-score is not redundant. `runs/run.lock` free at check, GPU idle (irrelevant, CPU-only), disk 186G free on `/cta/users`. Original `experiments/exp-004/exp-005/exp-008/exp-009/{config.json,metrics.json,notes.md}` are NOT touched — this row's artifacts land only under `experiments/exp-011/` (committed) and `runs/exp-011/` (gitignored). `runs/run.lock` acquired (`exp-011 2026-08-22T23:11:56+03:00`); GPU state not checked further (irrelevant, CPU-only). All 9 evaluator invocations run at `main` commit `f4a1a19` (clean tree throughout), no pipeline code or `configs/musique_eval.json` changed. Verified additive-only empirically (not just trusted from the evaluator's docstring): every re-scored arm's pre-existing headline metrics (`step_f1_macro`, `ordered_step_accuracy_macro`, `hop_count_exact_match_rate`, `composite_score`) reproduce the original log row's values exactly. `runs/run.lock` released after this entry and `experiments/exp-011/{config.json,metrics.json,notes.md}` were committed. Committed artifacts: `experiments/exp-011/{config.json,metrics.json,notes.md}`; heavy per-arm evaluator trail (`eval_metrics.json`/`eval_per_item.json`/`eval_notes.md`/`eval_config.json`/`eval.log` x9) stays on the box under `runs/exp-011/<arm>/`. |
| exp-012 | 2026-08-23 | experiment-runner:session-1 | Issue #27 / issue #46 S1 / ADR 0024: does the router earn its place — does feeding the decomposer a router-predicted hop count (`router_guided`) improve decomposition quality over no hop guidance at all (`unguided`), on the same pinned eval set, same retrieval artifact, same model, same decoding? Secondary: how much of `oracle_guided`'s already-logged gain (exp-004) survives an imperfect, prompted (not trained) router. Also produces the few-shot-prompted router's own hop-prediction accuracy as ADR 0024's candidate implementation. No keep/drop recommendation is made here (Jahid + supervisor's call, issue #46 §4.2). | Router: `components/router/run_router.py --model mistral_7b_instruct --config router_musique.json --output-root runs/exp-012/router` (retrieved-few-shot prompting, `configs/router_musique.json` unedited, retrieval artifact resolved from `retrieval.input_key: musique_few_shot_top5_pinned600`, no `--retrieval-input` override needed). Decomposer with-router arm: `components/decomposer/run_decomposer.py --model mistral_7b_instruct --config decomposer_musique.json --condition router_guided --hop-predictions <router predictions.jsonl> --output-root runs/exp-012/decomposer/router_guided` (same retrieval artifact and decoding as every other MuSiQue decomposer arm; `router_guided`'s only difference from `oracle_guided` is the hop-count source, per ADR 0024). Without-router side reuses exp-004's already-committed `unguided` and `oracle_guided` arms (`mistralai/Mistral-7B-Instruct-v0.3`, same config, same retrieval artifact, same seed) re-scored at this run's commit via `scripts/musique_decompositions_evaluator.py` (exp-011 precedent: additive-only re-score, no new decomposition needed) so all three per-item files carry the same evaluator version before `--compare`. Significance via `--compare` (paired bootstrap 95% CI + McNemar + paired t-test, ADR 0009/0011). Model scope: mistral_7b_instruct only in this run (matches exp-004/008/009/011's majority model and keeps the lane's GPU time bounded); Qwen3.5-9B router/router_guided arms are out of scope here, not attempted. Pool/retrieval note: the only committed retrieval artifact for the ADR 0007 pinned 600 is the single ADR 0014 v1 9,156-row pool (`musique_few_shot_top5_pinned600`) — the imbalanced/balanced/clustered pool-CONSTRUCTION axis (exp-010) is a separate, unrelated harness over a different 750-query dev sample at pool size 2000 and has no pinned-600 counterpart; this run uses the sole existing pinned-600 artifact unchanged (status quo, not an adoption of any pool strategy) for both arms. | c69ef5f34db07483cf486cf33baca2a71faaf72d | 42 | mistralai/Mistral-7B-Instruct-v0.3 (issue #12) | MuSiQue ADR 0007 pinned 600 (200/hop), retrieval artifact per ADR 0014 (v1 9,156-example pool, sha256 e5c418a9b25f...) | COMPLETE — rc=0, all 7 stages rc=0 (verified off run_all.rc and each stage's own rc= line independently, not just the orchestrator's final line). Router (mistral_7b_instruct, few-shot-prompted): overall_accuracy 0.3833 (230/600) vs 3-class chance ~0.333; per-hop (within-gold-class recall, not precision, ADR 0024 item 1a) hop=2 0.875 (175/200), hop=3 0.175 (35/200), hop=4 0.100 (20/200); 394/600 responses unparsed and scored at default_hop=2 (per-gold-hop 123/133/138), so hop=2's 0.875 is inflated — only 52/77 (0.675) of hop=2's model-parsed rows were correct, the rest of hop=2's "correct" count is 123 defaulted rows given free credit. Headline unguided vs router_guided (n=600): every one of 7 bootstrap metrics + 3 McNemar metrics non-significant — rouge_l_f1 0.5429 vs 0.5484 CI[-0.0144,+0.0032]; step_f1 0.2039 vs 0.2035 CI[-0.0103,+0.0113]; ordered_step_accuracy 0.1804 vs 0.1853 CI[-0.0160,+0.0060]; sari 0.5850 vs 0.5851 CI[-0.0077,+0.0075]; ged 0.4715 vs 0.4721 CI[-0.0163,+0.0152] (lower better); chain_validity 0.9686 vs 0.9625 CI[-0.0094,+0.0219]; composite_score 0.2098 vs 0.2150 CI[-0.2108,+0.0033] (recorded, NOT headlined — issue #40); exact_match 0.0617 vs 0.0583 McNemar p=0.7744 (b=7,c=5); hop_count_exact_match 0.5083 vs 0.4917 McNemar p=0.500 (b=94,c=84); break_exact_match 0.0533 vs 0.0533 p=1. Secondary oracle_guided vs router_guided (n=600): hop_count_exact_match 0.5900 vs 0.4917 McNemar p=9.69e-06 significant favouring oracle (b=117,c=58); ged 0.4414 vs 0.4721 CI[-0.0440,-0.0177] significant favouring oracle (lower better); step_f1 0.2061 vs 0.2035, ordered_step_accuracy 0.1852 vs 0.1853, sari 0.5890 vs 0.5851, chain_validity 0.9712 vs 0.9625 all non-significant; exact_match p=1, break_exact_match p=0.6875 non-significant; composite_score 0.4197 vs 0.2150 CI[+0.0001,+0.2103] technically excludes zero but NOT headlined (near-zero lower bound, issue #40's bimodal/asymmetric composite shape). GED fallback counts: router_guided {} 0/600; unguided_rescored {node_cap:1} 1/600 (gold hop=4; docs/METRICS.md: the node-cap bound is not tight, measured up to +0.0588 above the optimizer's value at the cap boundary); oracle_guided_rescored {} 0/600. Full numbers: experiments/exp-012/metrics.json, runs/exp-012/{router,eval,compare_*}. | No keep/drop recommendation drawn (Jahid + supervisor's call, ADR 0028 item 2, issue #46 §4.2): hop guidance from a PERFECT oracle moves only hop_count_exact_match and ged; step content quality (step_f1/ordered_step_accuracy/sari/chain_validity) is flat even under perfect guidance, and an imperfect prompted router (barely above chance) moves nothing significantly, including hop_count_exact_match itself. Drift-check argument (issue #27 comment 5383430711, ADR 0011) for reusing exp-004's unguided arm generated at 6fc4bba and re-scored here: run_decomposer.py's +283/-9 diff since then is entirely the router_guided addition and resolve_hop_source short-circuits on `if not guided:`; decomposer_musique.json's +14/-1 diff is additive only, unguided block and max_new_tokens=1024 untouched; mistral_7b_instruct prompt files have an empty diff (unguided prompt still hashes e9fef279...); retrieval artifact sha256 e5c418a9b25f... is byte-identical to exp-004's; eval set loaded 600/600, ids_missing 0/ids_unexpected 0. This is an argument from targeted diffs, not proof by re-generation (~1130s GPU not spent). Commit-provenance discrepancy found and resolved: the cited comment names the router_guided generation commit as c1369a9, but every on-box artifact of this run instead stamps c69ef5f (run_all.sh logs the launch commit 7314004 at start; each stage's own JSON calls git rev-parse HEAD at write time, and concurrent-lane docs/log commits landed on main during the run's ~55 min window); `git diff --stat` for components/configs/scripts/src is empty both from 7314004 and from c1369a9 to c69ef5f — no pipeline code changed, so the drift-check argument is unaffected; this row records c69ef5f. composite_score recorded per schema but not headlined (issue #40 reference_validity defect), same convention as exp-010/exp-011. Full detail: experiments/exp-012/notes.md. |
| exp-013 | 2026-08-23 | experiment-runner:session-1 | Issue #41 instrument 1 / ADR 0023 item 3 / issue #46 S3: Jahid's hypothesis is that the fine-tuned decomposer's win (exp-008) is dataset/distribution-bound, where dynamic prompting is not — a model trained only on 2-hop and 3-hop MuSiQue should generalise to 4-hop (held out from training) if it learned the task, and should lag if it memorised hop patterns. This run trains the already-designed `generalisation_2_3hop` arm and measures its 4-hop decomposition quality against the `unguided` dynamic-prompting baseline (exp-004, same model/config/retrieval/seed) on the same held-out 4-hop rows. Hypothesis, not conclusion — the numbers decide; no keep/drop or generalises/doesn't-generalise claim is asserted beyond what the significance test supports. | `components/decomposer/train_lora.py --arm generalisation_2_3hop` (`configs/finetune_decomposer.json`, train_hops=[2,3] from `musique_train`, eval_hops=[4], unedited) to produce the adapter; then `components/decomposer/run_decomposer.py --model mistral_7b_instruct --config decomposer_musique.json --condition unguided --adapter <adapter> --no-few-shot --output-root runs/exp-013/decomposer` over the FULL pinned 600 (same convention as exp-008 — satisfies the config's `eval_rows_per_hop`=200/200/200 assertion with no override flag, and gives a per-hop breakdown including the held-out hop=4 row in one evaluator pass); `scripts/musique_decompositions_evaluator.py --predictions <results.json> --run-dir runs/exp-013/eval --out-prefix eval` to score it. For the significance test proper (both sides hop=4 only, per ADR 0012 item 11's enforcement that a generalisation claim is only as good as the 4-hop restriction actually holding): the fine-tuned per-item file is filtered post-hoc to its 200 hop=4 items (`runs/exp-013/eval/eval_per_item_hop4_only.json`, item ids/scores byte-identical to the unfiltered file, no re-scoring); the baseline reuses the already-committed exp-004 `unguided` arm re-scored under the current (Break-faithful) evaluator at exp-011 (`runs/exp-011/exp004_unguided/eval_per_item.json`) filtered the same way to its 200 hop=4 items (`runs/exp-013/baseline_unguided_4hop_per_item.json`, built during this run's preflight, before the lock — no GPU, no re-generation). `scripts/musique_decompositions_evaluator.py --compare` then runs on the two filtered 200-item files (bootstrap 95% CI + McNemar + paired t-test, ADR 0009/0011). Seed 42 throughout (training and inference). | 6679fef | 42 | mistralai/Mistral-7B-Instruct-v0.3 + LoRA adapter (QLoRA r=16, trained this run, `generalisation_2_3hop` arm) vs baseline `mistralai/Mistral-7B-Instruct-v0.3` dynamic few-shot prompting (`unguided`, exp-004) | MuSiQue ADR 0007 pinned 600 for the fine-tuned arm's own full inference pass (per-hop breakdown); the held-out generalisation comparison itself is restricted to the 200 hop=4 rows of that same pinned set on both sides, retrieval artifact per ADR 0014 (v1 9,156-example pool, sha256 e5c418a9b25f...) | PENDING — waiter armed, training not yet started (GPU held by exp-012 at time of this entry) | PENDING — waiter armed, training not yet started (GPU held by exp-012 at time of this entry) | PREFLIGHT (this entry, before the run): arm verified present and configured exactly as issue #41 claims — `configs/finetune_decomposer.json` `generalisation_2_3hop`: `train_hops: [2, 3]`, `train_source_key: musique_train`, `eval_hops: [4]`; confirmed live via `train_lora.py --arm generalisation_2_3hop --dry-run`, which itself reports `examples=18763 hops={'2': 14376, '3': 4387} eval_ids=600 overlap=0`. Independently re-derived (not just trusted from the dry-run print): loaded `musique_ans_v1.0_train.jsonl` (19,938 rows; hop split by id prefix 14,376/4,387/1,175 for 2/3/4-hop) and the three ADR 0007 dev-sample files (200/200/200, 600 distinct ids, ids of the `2hop__`/`3hop_`/`4hop_` form); the 18,763 candidate training ids (2-hop+3-hop only) have **zero** overlap with any of: the 600 pinned eval ids as a whole, the 200 hop=4 ids specifically (the held-out set this run measures on), the 200 hop=2 ids, or the 200 hop=3 ids — train and eval come from disjoint MuSiQue splits (train vs dev) so this is expected, and it is now measured rather than assumed. Data paths resolve: `data_root=/cta/users/fyilmaz/thesis-qav2-data` (symlink layer, docs/compute.md), `musique_train` and the three `musique_eval_questions_template` files all present and readable at the row counts above. Disk: `/cta/users/fyilmaz/Thesis---QAv2` on `/cta/users` filesystem, 186G free (89% full) — headroom checked, matches exp-011's last recorded figure; checkpoints/adapter go to gitignored `runs/exp-013/`. `runs/run.lock` is currently held by `exp-012 2026-08-23T03:37:39+03:00` (the issue #27 router lane, `nvidia-smi` confirms a live compute process using ~14.3 GiB, not stale) — not touched, not waited on synchronously. A detached waiter (`runs/exp-013/waiter.sh`, log `runs/exp-013/waiter.log`) is running inside tmux session `exp-013-waiter`: it polls for the lock file to disappear (60s sleep, no busy-loop), then writes `exp-013 <ISO timestamp>` to `runs/run.lock` and runs the five steps in the approach/config column end to end, releasing the lock on completion or failure. Working tree was clean at HEAD `6679fef` (docs-only commits since the `exp-012` pre-run entry; no pipeline code or config touched) when the waiter was armed. `composite_score` will be recorded when the evaluator runs (per the config's `quality_keys`) but per issue #40's reference_validity defect (unrelated replacement metric work in a parallel lane) it will NOT be headlined here — `step_f1`/`ordered_step_accuracy`/`hop_count_exact_match` are. This row will be completed with metrics once the waiter's run finishes. |
| exp-014 | 2026-08-23 | experiment-runner:session-1 | Issue #46 S2 (CPU half) / ADR 0023 item 2.3 feasibility note (`docs/analysis/2026-08-23-s2-feasibility-prompting-vs-finetuned.md`, commit 6679fef): exp-011 re-scored nine pinned-600 arms under PR #44's additive Break-faithful columns (`break_exact_match`, `sari`, `ged`, `chain_validity`) but ran no `--compare` ("a coverage run, not a comparison"). This run closes that: paired significance (ADR 0009 bootstrap+McNemar, ADR 0017 paired t-test) on those four columns (plus the pre-existing ones, which `--compare` always reports together) for the headline pair exp-004 `unguided` vs exp-008 `full_train`, plus further same-eval-set pairs that are cheap on the already-computed exp-011 per-item files (exp-004's own triple, exp-005's triple, exp-009's pair). No new generation, no GPU, CPU-only. This run does NOT attempt ADR 0023 item 2.3's "best pool" comparison itself — no prompting arm exists yet over any size-2000 pool on the pinned 600 (feasibility note verdict (b)); this row separately builds the CPU retrieval-artifact input the GPU cell will need (not a logged experiment, ADR 0027 point 5 — its provenance is recorded in this row's notes instead), but does not run that GPU cell. | `.venv/bin/python scripts/musique_decompositions_evaluator.py --compare runs/exp-011/<A>/eval_per_item.json runs/exp-011/<B>/eval_per_item.json --run-dir runs/exp-014/compare/<A>_vs_<B> --out-prefix compare --seed 42` for pairs: (exp004_unguided, exp008_full_train) [headline], (exp004_unguided, exp004_oracle_guided), (exp004_unguided, exp004_unguided_capped), (exp004_oracle_guided, exp004_unguided_capped), (exp005_unguided, exp005_oracle_guided), (exp005_unguided, exp005_unguided_capped), (exp005_oracle_guided, exp005_unguided_capped), (exp009_mixed, exp009_oracle_hop_matched) — all read-only against exp-011's already-committed `runs/exp-011/<arm>/eval_per_item.json` files, `configs/musique_eval.json` unedited. Separately (no log entry per item, provenance recorded here): retrieval-artifact build for the S2 GPU cell's input, exp-007's exact recipe (`MusiQue/scripts/check_question_similarity.py --pool-file runs/pool_sweep/pools/size2000_imbalanced_trial0_poolseed42/pool.jsonl --query-glob musique/dev_data/musique_ans_v1.0_dev_sample_*_hop_200.jsonl --n 200 --mode typed --top-k 20 --device cpu --seed 42 --no-hop-match` then `rerank_similarity_results.py --rerank-k 5 --device cpu --seed 42`), pool per ADR 0028 item 1 (`size2000_imbalanced`, pre-existing, not rebuilt). | a6353d90518448e7358fb68a73bafaddaf026027 | 42 | none (re-scoring/comparing already-computed per-item files from mistralai/Mistral-7B-Instruct-v0.3 base+LoRA per exp-004/008/009; retrieval-artifact build uses `intfloat/e5-small-v2` bi-encoder + `cross-encoder/ms-marco-MiniLM-L-12-v2` reranker, no decomposer) | MuSiQue ADR 0007 pinned 600 (200/hop), same set for every pair compared here (all nine exp-011 arms share this id set, verified by exp-011's own row and re-verified by every `--compare` invocation's own id-alignment refusal-if-mismatched check) | COMPLETE — deliverable 1: 8/8 `--compare` invocations rc=0, n=600 aligned every pair, CPU-only. HEADLINE (exp004_unguided vs exp008_full_train): every metric significant favouring full_train — break_exact_match 0.0533 vs 0.1017 (McNemar p=8.17e-05, b=12,c=41); sari 0.5850 vs 0.6823 (CI[-0.1114,-0.0832]); ged (lower better) 0.4715 vs 0.3094 (CI[+0.1422,+0.1821], full_train's lower GED is the better result); chain_validity 0.9686 vs 0.9942 (CI[-0.0387,-0.0133]); step_f1 0.2039 vs 0.3411 (CI[-0.1601,-0.1150]); ordered_step_accuracy 0.1804 vs 0.3237 (CI[-0.1666,-0.1205]); hop_count_exact_match 0.5083 vs 0.7100 (McNemar p=1.12e-15, b=57,c=178); composite_score 0.2098 vs 0.5236 (CI[-0.3307,-0.1055], recorded, NOT headlined, issue #40). GED fallback counts: unguided 1/600, full_train 0/600 (negligible on both sides). FURTHER PAIRS (same eval set, all cheap CPU): exp004 unguided vs oracle_guided — only `ged` significant (0.4715 vs 0.4414, CI[+0.0167,+0.0437], favours oracle; fallback 1/600 vs 0/600), break_exact_match/sari/chain_validity not significant, reproducing exp-004's original shape (oracle moves hop-count-EM/composite, not step quality); exp004 unguided vs unguided_capped near-identical on all four new columns (ged CI[+0.0002,+0.0014], effect size noise-scale); exp004 oracle_guided vs unguided_capped mirrors the first pair. exp005 (Qwen3.5-9B) unguided vs oracle_guided — `ged` significant (0.4910 vs 0.4241, CI[+0.0510,+0.0832], favours oracle; fallback 0/600 vs 6/600, the largest fallback count in this study, flagged per docs/METRICS.md's up-to-+0.0588 substituted-bound bias — direction not put in question, magnitude caveated), break_exact_match significant by paired t-test (p=0.0252) but McNemar `underpowered` (b=0,c=5, min-attainable-p=0.0625); sari/chain_validity not significant; exp005 unguided vs unguided_capped identical to 3 decimals (cap fired 4/600); exp005 oracle_guided vs unguided_capped mirrors the oracle pair. exp009 mixed vs oracle_hop_matched — no new column reaches significance (ged CI[-0.0003,+0.0351] crosses zero; fallback 1/600 vs 1/600), hop_count_exact_match remains the only significant axis (McNemar p=6.16e-04), reproducing exp-009's original finding. Deliverable 2: retrieval artifact built CPU-only (no GPU, `runs/run.lock` held by exp-012 throughout, unchanged before/during/after) — pool `runs/pool_sweep/pools/size2000_imbalanced_trial0_poolseed42/pool.jsonl` (2000 rows, sha256 cc3646625d759a1b58179b18c25f83d255db24f05acc08b666985af2e58d6646, re-verified matching ADR 0028, not rebuilt), 0/2000 pool-vs-pinned-600 id overlap re-verified. Similarity stage (typed, top-k 20, n 200, seed 42, `intfloat/e5-small-v2`, on-the-fly NER masking since the dev sample lacks masked fields): 600/600 rows, 200/200/200 hop split, ~62.5s real -> `runs/exp-014/retrieval/s2_pinned600_top20.jsonl` sha256 493360b4d4e43e0badbdce1162834979dfe80964a0dcc1f0ff71797df4b5f4c4. Rerank stage (rerank-k 5, `cross-encoder/ms-marco-MiniLM-L-12-v2`, seed 42): 600/600 rows, 200/200/200 hop split, top-5/row, ~51.5s real -> **`runs/exp-014/retrieval/s2_pinned600_imbalanced_rerank_top5.jsonl` sha256 3f95c7dc18c14fc6bea3473f1be5c0d7fb21b7b2f2b2a1175dbfaebb5092fdb0**. Full tables: `experiments/exp-014/metrics.json`. | Preflight before deliverable 2: dry-run of `check_question_similarity.py` confirmed 600/600 pinned queries load, 2000-row pool, CPU, no model loaded; `runs/run.lock` and `nvidia-smi --query-compute-apps` checked before, mid- and after both retrieval stages — held by `exp-012 2026-08-23T03:37:39+03:00` throughout (pid 1301582, ~14.3 GiB), never touched, never waited on; this lane took **no GPU lock and no GPU** at any point (deliverable 1 is a CPU comparison over already-computed per-item files, deliverable 2 is a CPU bi-encoder+cross-encoder precompute over an already-built pool — same class of work exp-006/exp-007/exp-011 already established needs no GPU), so it did not contend with exp-012 or block the `exp-013-waiter` queued behind it. Deliverable 2 is explicitly NOT a logged experiment on its own (ADR 0027 point 5 — building an artifact scores no system); its provenance (pool path+sha256, recipe, seed, model identities, output path+sha256) is recorded in this row and in `experiments/exp-014/{config.json,metrics.json,notes.md}` for the later GPU run to cite. This row does NOT attempt ADR 0023 item 2.3's actual prompting-vs-fine-tuned-at-best-pool comparison — that needs the GPU decomposer cell consuming this artifact (`components/decomposer/run_decomposer.py --model mistral_7b_instruct --config decomposer_musique.json --condition unguided --seed 42 --retrieval-input runs/exp-014/retrieval/s2_pinned600_imbalanced_rerank_top5.jsonl`, exp-009's exact shape), which is out of scope for this CPU-only lane and is sequenced behind exp-012/exp-013 on `runs/run.lock`. Original `experiments/exp-004/005/008/009/010/011/` were not modified (read-only). Committed artifacts: `experiments/exp-014/{config.json,metrics.json,notes.md}`; heavy trails (`runs/exp-014/compare/**`, `runs/exp-014/retrieval/**`) stay on the box under gitignored `runs/`. |
| exp-010 | 2026-08-22 | experiment-runner:session-1 | Issue #14 / ADR 0021 GPU follow-up: whether the clustering pool-construction strategy (size 2000) matches, beats, or lags `imbalanced`/`balanced` on decomposition quality, at the same size, same 750-query dev sample, same two retriever variants and three retrieval modes, is unmeasured. exp-006 produced the CPU precompute (pool construction + bi-encoder similarity + CE rerank) for all 3 strategies; this run is the deferred GPU half — 18 decompose+eval cells (3 strategies x 2 retriever variants x 3 retrieval modes) — that actually produces the comparison issue #14 asks for. PRE-LAUNCH CHECK (required by the brief) verified with evidence before any cell was launched: the dev sample lacks `question_masked_typed`/`question_masked_uniform` fields (exp-006's own flag), but `check_question_similarity.py`'s own docstring says it NER-masks queries on the fly when those fields are missing; read directly off exp-006's committed `top20.jsonl` artifacts (all 3 balances), `typed_top_k`/`uniform_top_k` candidate-id lists differ from `raw_top_k` on **750/750 rows in every one of the 3 pool balances** (0 rows identical across all three modes) — so `typed`/`uniform` are genuinely distinct retrieval conditions here, not a silent fallback to raw, and all 18 cells are meaningful (none cut). Issue #14's own 2026-08-2x comment independently records the same finding. Also checked: `configs/pool_sweep.json` points `configs.decomposer` at the MetaQA-flavoured `configs/decomposer.json` (hops [1,2,3], `questions_template_key` metaqa) rather than `decomposer_musique.json`; traced in `run_decomposer.py` that with `--retrieval-input` supplied (which the orchestrator always passes for this sweep) the question-loading branch that uses `hops`/`questions_template_key` is never reached — hop comes from parsing the retrieval file's own `query_id` prefix — and the pinned-eval-set assertion is a no-op because `decomposer.json` declares no `eval_rows_per_hop` (verified in the runner's own source: an absent key short-circuits the check with `"this config declares no 'eval_rows_per_hop', so no pinned-set assertion applies"`). No `conditions` block in `decomposer.json` means the run behaves as the plain `guided: false` unguided arm (`resolve_condition`'s documented behaviour for a config without one). Not a blocker; recorded so the next reader does not mistake `decomposer.json` for a MuSiQue-shaped config by name alone. | `scripts/pool_sweep_orchestrator.py --stage decompose --only size2000_imbalanced,size2000_balanced,size2000_clustered` then `--stage eval` (same `--only`), against `configs/pool_sweep.json` (`decomposer_model_folder: mistral_7b_instruct`, `decomposer_quantization: 4bit`, `retrieval_k: 5`) unedited; `--dry-run` preflight run for both stages first (confirmed: dev sample and all CPU precompute skip via skip-if-exists — no similarity/pool/rerank command is even printed for `--stage decompose`/`--stage eval`, only the 18 decomposer/evaluator invocations — GPU/CPU precompute is never re-touched). Headline comparison after both stages complete: `scripts/musique_decompositions_evaluator.py --compare <a>_per_item.json <b>_per_item.json` pairwise across `imbalanced`/`balanced`/`clustered` at `biencoder_plus_ce` + `typed` (ADR 0006 item 4's stated default, applicable here since the pre-launch check confirms `typed` is genuinely distinct from `raw` for this dev sample). | 6831ee5 | 42 | mistralai/Mistral-7B-Instruct-v0.3 (`mistral_7b_instruct`, 4-bit; issue #12) | pool_sweep's own dev sample (`dev_seed 42`, `dev_per_hop 250`, coarse buckets 2hop/3hop/4hop = 750 queries; distinct from the ADR 0007 pinned 600 used by exp-002..005/008/009) | COMPLETE — all 18 decompose+eval cells rc=0, 750/750 rows evaluated on every cell, 0 missing gold. Headline cell (`biencoder_plus_ce` + `typed`, ADR 0006 item 4 default) overall step_f1/ordered_step_accuracy/hop_count_exact_match/composite (composite recorded, NOT headlined — issue #40): imbalanced 0.1900/0.1706/0.5133/0.2001; balanced 0.1926/0.1710/0.4667/0.1974; clustered 0.1939/0.1775/0.5133/0.2038. Pairwise `--compare` (bootstrap 95% CI + McNemar + paired t-test, seed 42, n=750 aligned): step_f1/ordered_step_accuracy/rouge_l_f1/exact_match NOT significant in any of the 3 pairs (all CIs cross 0). hop_count_exact_match_rate is the one significant axis: imbalanced vs balanced McNemar p=0.0438 (t p=0.0381), balanced vs clustered McNemar p=0.0380 (t p=0.0328) — imbalanced (0.5133) and clustered (0.5133, exactly tied) both significantly beat balanced (0.4667); imbalanced vs clustered indistinguishable (identical rate, McNemar p=1.0). Per-hop (step_f1/ordered_step_accuracy/hop_count_EM) hop=2: imbalanced 0.302/0.290/0.756, balanced 0.282/0.268/0.584, clustered 0.320/0.310/0.716 — the hop-count gap concentrates here; hop=3: imbalanced 0.151/0.127/0.472, balanced 0.174/0.152/0.504, clustered 0.152/0.129/0.552 — clustered leads hop-count, step metrics flat; hop=4: imbalanced 0.117/0.095/0.312, balanced 0.122/0.093/0.312, clustered 0.110/0.093/0.272 — all converge (smallest pool bucket in every strategy). Reading: pool-construction strategy moves whether the model states the right step COUNT, not whether the steps it emits are the right ones — same shape as exp-005/exp-009 on unrelated axes. No claim that clustering beats imbalanced: statistically indistinguishable here; both beat balanced only on hop-count. Full 18-cell table, per-hop breakdown and raw `--compare` JSONs: `experiments/exp-010/metrics.json`. | Rc verified independently off every eval_metrics.json (not just the orchestrator trail): 18/18 decompose rc=0 (~1515–1792s/cell, ~8h10m total), 18/18 eval rc=0 (~5–9s/cell); wall clock 2026-08-22T11:23:00Z (lock acquired) → 19:33:08Z (run.log). SHARED-CHECKOUT COMPLICATION, resolved with evidence (see `experiments/exp-010/config.json`): this run's 8h10m span crossed three self-merged PRs (`fe4b32f` #43 router readiness, `a940376` #42 end-to-end backends, `a99b573` #44 additive Break-faithful metrics) plus 2 docs-only commits; each of the orchestrator's 36 subprocess calls is spawned fresh and reads current disk state, so imbalanced ran entirely pre-merge/docs-only, balanced split across docs-only and `a940376`, clustered ran entirely on `a99b573`, and all 18 eval cells (one contiguous burst after 19:31 UTC) carry `a99b573`'s new additive columns. Verified via targeted `git diff` that none of this touches a value this row reports: `configs/pool_sweep.json`/`similarity.json`/`musique_prep.json`/the mistral model config/prompt files are byte-identical across the whole range; `run_decomposer.py`'s only change (`resolve_hop_source`, #43) is gated on `guided=true` or an explicit `hop_source` condition key — neither set by this sweep's unguided config — so it is a documented no-op; the evaluator's #44 change is additive by its own docstring and `_statistics_available()`'s missing-column handling, with `composite_score_weights` byte-identical in `configs/musique_eval.json`. Per the coordinator's explicit instruction this is NOT a re-evaluation under the new metrics (the 4 new columns are recorded per schema in `eval_metrics.json` but unused in this row's comparison) — the nine-arm re-score under the new metrics is a separate, later assignment. `runs/run.lock` released after this entry and its artifacts were committed. Committed artifacts: `experiments/exp-010/{config.json,metrics.json,notes.md}`. |
| exp-015 | 2026-08-23 | experiment-runner:session-1 | Issue #46 S2 GPU cell / ADR 0023 item 2.3, per `docs/analysis/2026-08-23-s2-feasibility-prompting-vs-finetuned.md` (commit `6679fef`) §3 and ADR 0028 item 1: is dynamic few-shot prompting (Mistral-7B-Instruct-v0.3, `unguided`) at its **best pool** (`size2000_imbalanced`, the lead's ADR 0028 item 1 delegated choice) different from the fine-tuned decomposer (exp-008 `full_train`) on the ADR 0007 pinned 600 — same eval set, same decoding, same condition as exp-004/exp-008, with only the few-shot pool behind the retrieval artifact moved. exp-009's exact shape with only `--retrieval-input` swapped to exp-014's freshly built pinned-600-over-size2000_imbalanced artifact. exp-008 needs no re-run (`no_few_shot: true`, so the pool decision cannot touch it; its existing per-item file is the fine-tuned side of the `--compare`). | `.venv/bin/python components/decomposer/run_decomposer.py --model mistral_7b_instruct --config decomposer_musique.json --condition unguided --seed 42 --retrieval-input runs/exp-014/retrieval/s2_pinned600_imbalanced_rerank_top5.jsonl --output-root runs/exp-015/decomposer` then `.venv/bin/python scripts/musique_decompositions_evaluator.py --predictions <results.json> --run-dir runs/exp-015/eval --out-prefix eval --seed 42` then `.venv/bin/python scripts/musique_decompositions_evaluator.py --compare runs/exp-015/eval/eval_per_item.json runs/exp-011/exp008_full_train/eval_per_item.json --run-dir runs/exp-015/compare_vs_exp008_full_train --out-prefix compare --seed 42` (`configs/decomposer_musique.json`/`configs/musique_eval.json` unedited; nothing changed but the `--retrieval-input` path). Frozen per the feasibility note's §3 checklist, verified this session against the committed configs before arming (not taken on trust): `quantization: "none"` (`components/decomposer/models/mistral_7b_instruct/config.json` → `loader.quantization`, not the pool-sweep's 4-bit), `generation_overrides.max_new_tokens: 1024` + `do_sample: false`/`temperature: 0.0`/`top_p: 1.0` (`decomposer_musique.json` + model config), condition `unguided`, `prompt_unguided.md` sha256 `e9fef279e7c21a313663e14e1884cda5aed9dc11d7f0134e828c1948be514ddf` (matches the note's `e9fef279…` prefix exactly), `few_shot.k: 3`, `retrieval.mode: "typed"`, `retrieval.k: 5`, self-exclusion on (unconditional in `run_decomposer.py`'s `normalize_for_self_exclusion` path, not a togglable config key), seed 42 throughout. Retrieval artifact verified independently before arming: `runs/exp-014/retrieval/s2_pinned600_imbalanced_rerank_top5.jsonl`, sha256 `3f95c7dc18c14fc6bea3473f1be5c0d7fb21b7b2f2b2a1175dbfaebb5092fdb0` (exact match to both the brief and ADR 0028/issue #46 comment's cited hash), 600 rows, hop split 200/200/200 (independently recounted by parsing `query_id` prefixes, not trusted from provenance text alone). | c69ef5f34db07483cf486cf33baca2a71faaf72d | 42 | mistralai/Mistral-7B-Instruct-v0.3 (`mistral_7b_instruct`, `quantization: none`) vs the already-committed fine-tuned arm `runs/exp-011/exp008_full_train/eval_per_item.json` (LoRA `full_train`, exp-008, not re-run) | MuSiQue ADR 0007 pinned 600 (200/hop), retrieval artifact `runs/exp-014/retrieval/s2_pinned600_imbalanced_rerank_top5.jsonl` (sha256 above) over the `size2000_imbalanced` pool (ADR 0028 item 1; pool sha256 `cc3646625d759a1b58179b18c25f83d255db24f05acc08b666985af2e58d6646`, 0/2000 overlap with the pinned 600) | PENDING — waiter armed, GPU lock held by `exp-013` (`generalisation_2_3hop`, Jahid's stated top-priority run, ADR 0028 item 4) at arming time; this run does not preempt it | PENDING — waiter armed, run not yet started | PREFLIGHT (this entry, before the run): every frozen parameter above checked directly against the committed config files this session, not assumed from the feasibility note's prose — all held. `runs/run.lock` read at arming time: `exp-013 2026-08-23T04:34:27+0300` (matches the brief's stated acquisition time), confirmed live via `nvidia-smi --query-compute-apps` (one active compute process, `.venv/bin/python`). Confirmed exactly one other lock-waiter exists on the box (`tmux ls`: `exp-013-waiter`, armed 03:45:27) and it will have exited by the time this run's own waiter fires (it releases the lock itself on completion or failure per `runs/exp-013/waiter.sh`) — no race between two armed waiters. A detached waiter (`runs/exp-015/waiter.sh`, log `runs/exp-015/waiter.log`) runs inside tmux session `exp-015-waiter`: polls for `runs/run.lock` to disappear (60s sleep, no busy-loop), writes `exp-015 <ISO timestamp>` to the lock, runs decompose → evaluate → `--compare` end to end (exp-009's pattern), records `ged_fallback_counts` beside `ged_macro` per docs/METRICS.md, and releases the lock unconditionally (completion or failure) — never touches `exp-013`'s process or lock while it is held. Disk 186G free on `/cta/users` (89% full, matches exp-011/exp-013's last recorded figures) — headroom checked. Working tree clean at HEAD `c69ef5f` when this entry and the waiter were committed/armed; no pipeline code or config touched by this lane. This row will be completed with metrics (headline `--compare` vs exp-008 `full_train` on the pre-existing columns plus the four Break-faithful columns, `ged_fallback_counts` reported beside `ged_macro`, `composite_score` recorded per issue #40 but not headlined) once the waiter's run finishes. |
| exp-016 | 2026-08-23 | experiment-runner:session-1 | Issue #40 / PR #48 follow-up (Refs #40): PR #48 (merged `e3725c7`) made the six-term unblended `decomposition_report_card` (break_exact_match_rate, sari_macro, ged_macro ⇓, chain_validity_macro, hop_count_exact_match_rate, over/under-decomposition rates) the reported primary and froze `composite_score` byte-identical as legacy (ADR 0029). Its Gate-1 review closed with one open nit: the nine arms' report-card values (already measured once, at commit `87e3765`/`f3b4d87`, for ADR 0029's junk-battery table) live only in that ADR, not in a log entry — a metric claim without a log entry is not citable (CLAUDE.md evidence discipline). exp-012's `router_guided` arm and its `unguided`/`oracle_guided` re-scores were also scored (at `c69ef5f`) before the report card landed. This run closes both gaps: re-score all 12 already-generated arms' committed `results.json` files at current `main` so `decomposition_report_card` exists for each, and confirm the re-score is additive (every pre-existing metric value reproduces exactly). Coverage only — no new generation, no new comparison claims. | `.venv/bin/python scripts/musique_decompositions_evaluator.py --config musique_eval.json --predictions <arm results.json> --run-dir runs/exp-016/<arm> --out-prefix eval --seed 42` (config/gold/composite weights unedited, `configs/musique_eval.json`), once per arm, all 12: exp004_unguided (`runs/exp-004/20260821_222907/results.json`), exp004_oracle_guided (`.../20260821_224805/results.json`), exp004_unguided_capped (`.../20260821_231641/results.json`), exp005_unguided (`runs/exp-005/20260821_233524/results.json`), exp005_oracle_guided (`.../20260822_000025/results.json`), exp005_unguided_capped (`.../20260822_003156/results.json`), exp008_full_train (`runs/exp-008/20260822_100745/results.json`), exp009_mixed (`runs/exp-009/mixed/20260822_103146/results.json`), exp009_oracle_hop_matched (`runs/exp-009/oracle/20260822_105045/results.json`), exp012_router_guided (`runs/exp-012/decomposer/router_guided/20260823_010750/results.json`), exp012_unguided_rescored (`runs/exp-004/20260821_222907/results.json` — exp-012's own row records this arm as a reuse of exp-004 `unguided`'s predictions, not a new generation), exp012_oracle_guided_rescored (`runs/exp-004/20260821_224805/results.json` — same reuse, of exp-004 `oracle_guided`). No `--compare` (no new cross-arm significance claims; exp-011/exp-012/exp-014 already hold those on their own eval sets). Per-arm preflight before scoring: each `results.json`'s parent run's own `metrics.json.evaluation_set` checked for `pinned: true`, `ids_missing_count: 0`, `ids_unexpected_count: 0`, `rows_loaded_per_hop: {2:200,3:200,4:200}` (all 12 passed; none excluded). **CPU-only, no GPU, no `runs/run.lock` taken** — this is a read-only re-score of already-computed prediction files against gold, the same class of work exp-006/exp-007/exp-011/exp-014 already established needs no GPU (exp-014 precedent for the exact wording); `runs/run.lock` was held by `exp-013` (`generalisation_2_3hop`, Jahid's stated top-priority training run) with `exp-015`'s waiter armed behind it throughout this lane's work, neither touched, neither waited on. | e3725c778b5c9869ac3f0b53a9430a40b675dba8 | 42 | none (re-scoring already-computed predictions; original arms' models: mistralai/Mistral-7B-Instruct-v0.3 base/LoRA per exp-004/008/009/012, Qwen/Qwen3.5-9B per exp-005) | Same as each arm's original row: MuSiQue ADR 0007 pinned 600 (200/hop) for all 12 arms (verified per-arm before launch via each arm's own generation-time `metrics.json.evaluation_set` record, not just a row count) | COMPLETE — 12 of 12 arms re-scored, all rc=0, 600/600 rows evaluated, 0 missing gold on every arm; every re-scored arm reproduces its most recent prior committed metrics exactly (`step_f1_macro`, `ordered_step_accuracy_macro`, `hop_count_exact_match_rate`, `composite_score`, `exact_match_rate`, `rouge_l_f1_macro`, plus the Break-faithful columns and the over/under-decomposition rates on the arms whose prior file already carried them — verified byte-for-byte against `runs/exp-011/<arm>/eval_metrics.json` for the 9 exp-004/005/008/009 arms and against exp-012's own committed `runs/exp-012/eval/{router_guided,unguided_rescored,oracle_guided_rescored}/eval_metrics.json` for the 3 exp-012 arms — no value failed to reproduce). Six-term report card per arm (break_EM ⇑ / SARI ⇑ / GED ⇓ / chain_validity ⇑ / hop_EM ⇑ / under ⇓ / over ⇓; ged_fallback_counts beside GED; decomp_mean = the non-suite contingency blend; composite_score recorded, NOT headlined, issue #40): exp004_unguided 0.0533/0.5850/0.4715 (fallback {node_cap:1})/0.9686/0.5083/0.2017/0.2900 (decomp_mean 0.5288, composite 0.2098); exp004_oracle_guided 0.0500/0.5890/0.4414 ({})/0.9712/0.5900/0.0617/0.3483 (0.5517, composite 0.4197); exp004_unguided_capped 0.0533/0.5849/0.4708 ({})/0.9686/0.5083/0.2017/0.2900 (0.5289, composite 0.2128); exp005_unguided 0.0517/0.5979/0.4910 ({})/0.8605/0.5200/0.1150/0.3650 (0.5085, composite 0.4212); exp005_oracle_guided 0.0600/0.6033/0.4241 (fallback {node_cap:6}, the largest count in this table)/0.8697/0.8733/0.0333/0.0933 (0.5965, composite 0.4429); exp005_unguided_capped 0.0517/0.5978/0.4909 ({})/0.8605/0.5200/0.1150/0.3650 (0.5085, composite 0.4215); exp008_full_train 0.1017/0.6823/0.3094 ({})/0.9942/0.7100/0.2567/0.0333 (0.6357, composite 0.5236); exp009_mixed 0.0533/0.5851/0.4713 (fallback {node_cap:1})/0.9686/0.5150/0.2000/0.2850 (0.5301, composite 0.2099); exp009_oracle_hop_matched 0.0483/0.5879/0.4540 (fallback {node_cap:1})/0.9686/0.6000/0.1300/0.2700 (0.5503, composite 0.2633); exp012_router_guided 0.0533/0.5851/0.4721 ({})/0.9625/0.4917/0.2917/0.2167 (0.5241, composite 0.2150); exp012_unguided_rescored 0.0533/0.5850/0.4715 (fallback {node_cap:1})/0.9686/0.5083/0.2017/0.2900 (0.5288, composite 0.2098) — byte-identical to exp004_unguided, as expected (same `results.json`, per exp-012's own row); exp012_oracle_guided_rescored 0.0500/0.5890/0.4414 ({})/0.9712/0.5900/0.0617/0.3483 (0.5517, composite 0.4197) — byte-identical to exp004_oracle_guided, same reason. GED fallback (all `node_cap`, deterministic, none `time_budget`/`no_optimizer_result`): 0/600 on 7 of 12 arms, 1/600 on exp004_unguided/exp009_mixed/exp009_oracle_hop_matched/exp012_unguided_rescored, 6/600 on exp005_oracle_guided (docs/METRICS.md: node-cap bound not tight, up to +0.0588 above optimizer value at the cap boundary — flagged as a small systematic worse-GED bias, not a correctness issue). ADR 0029's junk-battery table independently reproduced the same 9-arm values (measured at `87e3765`); this row is the first log entry to carry them. Descriptive-only reading (no `--compare` run here, no new significance testing; exp-011/exp-012/exp-014 already hold the paired tests on this eval set): exp008_full_train leads every report-card term by a wide margin, consistent with exp-011/exp-014's already-logged significant advantage; chain_validity separates by base model (all 3 exp-005/Qwen3.5-9B arms 0.8605–0.8697, below every Mistral-7B arm 0.9625–0.9942), reproducing exp-011's observation; exp012_router_guided sits close to unguided on most terms but has the lowest hop_count_exact_match (0.4917) and the highest under-decomposition rate (0.2917) in the table, consistent with exp-012's already-logged finding that the prompted router does not measurably help. Full per-arm tables, predictions sha256, and the reproduction-check detail: `experiments/exp-016/metrics.json`. | Preflight before scoring: all 12 `results.json` located and independently row-counted (600/600, 200/200/200 by `query_id` hop prefix) and cross-checked against each arm's own generation-time `metrics.json.evaluation_set` record (`pinned: true`, `ids_missing_count: 0`, `ids_unexpected_count: 0` on all 12 — none excluded). Confirmed `exp012_unguided_rescored`/`exp012_oracle_guided_rescored` point at byte-identical `results.json` paths to `exp004_unguided`/`exp004_oracle_guided` (read off exp-012's own committed `runs/exp-012/eval/{unguided_rescored,oracle_guided_rescored}/eval_metrics.json` `predictions_path` field) before scoring, so their reproduction was expected rather than a new finding. `runs/run.lock` checked before, during and after: held by `exp-013 2026-08-23T04:34:27+0300` throughout (confirmed live via `nvidia-smi`, one active compute process ~5.65 GiB), `exp-013-waiter` and `exp-015-waiter` tmux sessions present and not touched — this lane took **no GPU lock and no GPU at any point** (a read-only re-score of already-computed `results.json` predictions against gold, the same class of work exp-006/exp-007/exp-011/exp-014 already established needs no GPU; exp-014 is the precedent for this wording), so it did not contend with exp-013 or block exp-015's queued waiter. Disk 186G free on `/cta/users` (89% full, matches exp-011/exp-013/exp-014/exp-015's last recorded figure). All 12 evaluator invocations run at `main` commit `e3725c7` (clean tree throughout), no pipeline code or `configs/musique_eval.json` changed. Original `experiments/exp-004/`, `exp-005/`, `exp-008/`, `exp-009/`, `exp-011/`, `exp-012/` directories were NOT touched — this row's artifacts land only under `experiments/exp-016/` (committed) and `runs/exp-016/` (gitignored: `eval_metrics.json`/`eval_per_item.json`/`eval_notes.md`/`eval_config.json`/log per arm). No `runs/run.lock` was ever acquired or released by this lane. Committed artifacts: `experiments/exp-016/{config.json,metrics.json,notes.md}`. |
