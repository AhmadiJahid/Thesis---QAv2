<!-- Generated by Codery — setup session with Tural, 2026-08-11. Amend via issues in this repo. -->

# Experiment log

The single record of every experiment. **Append-only.** One row per experiment, ids `exp-001` onward.

## Rules

- **An entry is REQUIRED before a run starts** (hypothesis + config) and **completed after** (metrics). No experiment exists unless it is in this log.
- **No metric claim anywhere without a log entry** to cite.
- A **"this improved"** claim requires the *same evaluation set* as the baseline it is compared against.
- This log is how parallel sessions coordinate — read the last entries before starting any experiment work.

## Schema

| id | date | owner (human:session or agent) | hypothesis | approach/config | commit | seed | model | eval set | metrics | notes / learnings |
|---|---|---|---|---|---|---|---|---|---|---|

<!-- Illustrative row (delete this comment block when the first real entry lands):
| exp-001 | 2026-08-11 | jahid:session-1 | <one line: what you expect this run to show> | <approach>, configs/exp-001.yaml | abc1234 | 42 | 8B instruct (≤8B cap) | MuSiQue | EM — / ROUGE-F1 — / step P — R — | what the run showed, and anything the next reader needs to know |
-->

## Experiments

| id | date | owner (human:session or agent) | hypothesis | approach/config | commit | seed | model | eval set | metrics | notes / learnings |
|---|---|---|---|---|---|---|---|---|---|---|
| exp-001 | 2026-08-19 | experiment-runner:session-1 | LoRA fine-tuning on the full MuSiQue train split is a comparison arm against the prompting method; this run produces the trained adapter and its training-side record. | `configs/finetune_decomposer.json` arm `full_train` (full MuSiQue train split, no cap, no hop filter), via `components/decomposer/train_lora.py --arm full_train` | 2f49c20 | 42 | mistralai/Mistral-7B-Instruct-v0.3 (4-bit QLoRA, LoRA r=16) | MuSiQue ADR 0007 pinned 600 (200/hop) — evaluation deferred to a later run | training completed 2026-08-19 05:42 — train loss 0.140, 2 epochs / 2494 steps, 19,938 examples, wall clock 15,497.5 s, peak GPU 5.05 GiB; decomposition quality UNMEASURED (deferred to the eval run per design) | Training-only run (issue #13). Adapter at `runs/exp-001/20260818_222259/adapter`; committed artifacts in `experiments/exp-001/`. Run notes flag the working tree dirty by one untracked file (`Plan after the meeting.md`) at launch — code itself was at 2124333. Evaluation (quality + cost vs prompting baseline) happens in a separate later run via `scripts/compare_decomposer_arms.py`. `pool_2000` arm is BLOCKED on Jahid's "best pool" decision (issue #13, 2026-08-19 comment); `generalisation_2_3hop` queued to run after this one. |
| exp-002 | 2026-08-19 | experiment-runner:session-1 | If the oracle hop count materially improves decomposition quality over unguided, a router is justified. If unguided_capped closes most of that gap, the problem was runaway generation length rather than hop ignorance. | `configs/decomposer_musique.json`, all three conditions (`unguided`, `oracle_guided`, `unguided_capped`) via `components/decomposer/run_decomposer.py --model mistral_7b_instruct --config decomposer_musique.json --condition <cond>` | b6329ca (pipeline code 1ccea11) | 42 | mistralai/Mistral-7B-Instruct-v0.3 (issue #12) | MuSiQue ADR 0007 pinned 600 (200/hop), retrieval artifact per ADR 0014 (v1 9,156-example pool, sha256 e5c418a9b25f...) | FAILED — all 3 arms rc=1 at launch (2026-08-19 13:37:56), `KeyError: 'raw'` at `run_decomposer.py:1899` (per-query dump read `gen["raw"]`; `generate()` returns `"text"`). Zero decompositions produced; unmeasured. | Root cause: a dry run has `gen = None`, so preflight (dry-run) could not reach the crashing branch — only real generation hit it. Combined log: `runs/exp-002-003-combined.log`. Fixed in `bfbef15` (PR #26, Gate-1 reviewed, AST contract test added); main now at `8bcf70b`. Relaunched as `exp-004` (this model) per the fix. |
| exp-003 | 2026-08-19 | experiment-runner:session-1 | If the oracle hop count materially improves decomposition quality over unguided, a router is justified. If unguided_capped closes most of that gap, the problem was runaway generation length rather than hop ignorance. | `configs/decomposer_musique.json`, all three conditions (`unguided`, `oracle_guided`, `unguided_capped`) via `components/decomposer/run_decomposer.py --model qwen3_5_9b --config decomposer_musique.json --condition <cond>` | b6329ca (pipeline code 1ccea11) | 42 | Qwen/Qwen3.5-9B (admitted above the ~8B ceiling by ADR 0015, pending supervisor confirmation; issue #12) | MuSiQue ADR 0007 pinned 600 (200/hop), retrieval artifact per ADR 0014 (v1 9,156-example pool, sha256 e5c418a9b25f...) | FAILED — all 3 arms rc=1 at launch (2026-08-19 13:37:56), `KeyError: 'raw'` at `run_decomposer.py:1899` (per-query dump read `gen["raw"]`; `generate()` returns `"text"`). Zero decompositions produced; unmeasured. | Root cause: a dry run has `gen = None`, so preflight (dry-run) could not reach the crashing branch — only real generation hit it. Combined log: `runs/exp-002-003-combined.log`. Fixed in `bfbef15` (PR #26, Gate-1 reviewed, AST contract test added); main now at `8bcf70b`. Relaunched as `exp-005` (this model) per the fix. Qwen arms are out-of-scope-pending if the supervisor reasserts the 8B ceiling (ADR 0015). |
| exp-004 | 2026-08-19 | experiment-runner:session-1 | If the oracle hop count materially improves decomposition quality over unguided, a router is justified. If unguided_capped closes most of that gap, the problem was runaway generation length rather than hop ignorance. | `configs/decomposer_musique.json`, all three conditions (`unguided`, `oracle_guided`, `unguided_capped`) via `components/decomposer/run_decomposer.py --model mistral_7b_instruct --config decomposer_musique.json --condition <cond>` | 8bcf70b (pipeline code bfbef15, fix for #26; also carries #25 adapter guards) | 42 | mistralai/Mistral-7B-Instruct-v0.3 (issue #12) | MuSiQue ADR 0007 pinned 600 (200/hop), retrieval artifact per ADR 0014 (v1 9,156-example pool, sha256 e5c418a9b25f...) | COMPLETE — all 3 arms rc=0, 600/600 rows, 0 empty decompositions. Overall composite/step_f1/ordered_step_acc/hop_count_EM: unguided 0.2098/0.2039/0.1804/0.5083; oracle_guided 0.4197/0.2061/0.1852/0.5900; unguided_capped 0.2128/0.2040/0.1805/0.5083 (step-line cap fired on 12/600 rows, 7 actually cut off). Per-hop (over/under/step_count_MAE/mean_signed_error) hop=2/3/4: unguided 0.280/0.010/0.450/+0.430, 0.290/0.195/0.705/+0.315, 0.300/0.400/1.170/+0.260; oracle_guided 0.150/0.000/0.170/+0.170, 0.370/0.065/0.560/+0.430, 0.525/0.120/0.920/+0.680; unguided_capped 0.280/0.010/0.420/+0.400, 0.290/0.195/0.665/+0.275, 0.300/0.400/0.975/+0.065. --compare (n=600, bootstrap 95% CI + McNemar + paired t-test): unguided vs oracle_guided — step_f1 diff -0.0022 CI[-0.0129,+0.0085] not sig (t p=0.686); ordered_step_acc -0.0048 CI[-0.0155,+0.0061] not sig (t p=0.378); composite -0.2098 CI[-0.2189,-0.0058] sig; hop_count_EM -0.0817 McNemar p=0.000241 sig, t p=0.000182 sig; exact_match +0.0050 McNemar p=0.581 not sig. unguided vs unguided_capped: all quality metrics tie (composite -0.0030 CI[-0.0072,-0.0004] sig, driven by step-count-error term only; exact_match/hop_count_EM identical, 0 discordant pairs). oracle_guided vs unguided_capped mirrors unguided-vs-oracle in sign, oracle ahead. Cost/query (prompt+completion tok, latency s): unguided 420.4+79.3=499.8tok/1.879s; oracle_guided 475.4+120.5=595.9tok/2.844s; unguided_capped 420.4+77.4=497.9tok/1.857s. Full per-hop + comparison JSONs: `experiments/exp-004/metrics.json`. | Relaunch of exp-002 at the fixed SHA (`gen["raw"]` -> `gen["text"]`, PR #26). Dry-run preflight re-run at this SHA for all 3 conditions before launch: 600/600 pinned ids, 200/200/200 per hop, retrieval sha256 matches ADR 0014. GPU contended at launch time by another user's ollama process (~21.2 GiB of 24 GiB); launch waits for headroom per docs/compute.md. Generation completed overnight 2026-08-21/22 via the rearmed day-2 waiter; evaluated at commit 0068842 (no pipeline code changed since generation at 6fc4bba). Reading: on Mistral-7B, oracle guidance moves hop-count-EM and composite significantly but NOT step_f1/ordered_step_accuracy (CIs straddle zero, t-tests not significant) — helps get the step *count* right more often at +19% tokens/+51% latency, without a measured step-quality gain. unguided_capped is statistically indistinguishable from unguided (cap fired on only 12/600 rows) — this run cannot separate "runaway length" from "hop ignorance" as the dominant failure mode. No router-design recommendation made; that decision is Jahid's/the supervisor's. Committed artifacts: `experiments/exp-004/{config.json,metrics.json,notes.md}`. |
| exp-005 | 2026-08-19 | experiment-runner:session-1 | If the oracle hop count materially improves decomposition quality over unguided, a router is justified. If unguided_capped closes most of that gap, the problem was runaway generation length rather than hop ignorance. | `configs/decomposer_musique.json`, all three conditions (`unguided`, `oracle_guided`, `unguided_capped`) via `components/decomposer/run_decomposer.py --model qwen3_5_9b --config decomposer_musique.json --condition <cond>` | 8bcf70b (pipeline code bfbef15, fix for #26; also carries #25 adapter guards) | 42 | Qwen/Qwen3.5-9B (admitted above the ~8B ceiling by ADR 0015, pending supervisor confirmation; issue #12) | MuSiQue ADR 0007 pinned 600 (200/hop), retrieval artifact per ADR 0014 (v1 9,156-example pool, sha256 e5c418a9b25f...) | COMPLETE — all 3 arms rc=0, 600/600 rows, 0 empty decompositions. Overall composite/step_f1/ordered_step_acc/hop_count_EM: unguided 0.4212/0.2131/0.1911/0.5200; oracle_guided 0.4429/0.2368/0.2205/0.8733; unguided_capped 0.4215/0.2131/0.1911/0.5200 (step-line cap fired on 4/600 rows, 2 actually cut off). Per-hop (over/under/step_count_MAE/mean_signed_error) hop=2/3/4: unguided 0.280/0.005/0.335/+0.325, 0.400/0.130/0.695/+0.435, 0.415/0.210/0.895/+0.435; oracle_guided 0.025/0.000/0.025/+0.025, 0.105/0.060/0.745/+0.625, 0.150/0.040/0.845/+0.765; unguided_capped identical to unguided at hop 2/3, hop=4 0.415/0.210/0.870/+0.410. --compare (n=600, bootstrap 95% CI + McNemar + paired t-test): unguided vs oracle_guided — step_f1 diff -0.0237 CI[-0.0351,-0.0123] sig (t p=4.8e-05); ordered_step_acc -0.0293 CI[-0.0403,-0.0188] sig (t p=1.3e-07); composite -0.0217 CI[-0.0348,-0.0070] sig; hop_count_EM -0.3533 McNemar p=5.0e-48 sig, t p=1.0e-48 sig; exact_match -0.0117 McNemar p=0.0391 sig, t p=0.0195 sig — unlike Mistral, EVERY quality metric moves significantly in oracle_guided's favour on this model. unguided vs unguided_capped: statistically indistinguishable on every metric (cap fired on only 4/600 rows). oracle_guided vs unguided_capped mirrors unguided-vs-oracle in sign and magnitude, oracle ahead on every metric. Cost/query (prompt+completion tok, latency s): unguided 398.4+34.8=433.2tok/2.479s; oracle_guided 446.4+44.2=490.5tok/3.129s; unguided_capped 398.4+34.8=433.1tok/2.474s. Full per-hop + comparison JSONs: `experiments/exp-005/metrics.json`. | Relaunch of exp-003 at the fixed SHA (`gen["raw"]` -> `gen["text"]`, PR #26). Dry-run preflight re-run at this SHA for all 3 conditions before launch: 600/600 pinned ids, 200/200/200 per hop, retrieval sha256 matches ADR 0014. GPU contended at launch time by another user's ollama process (~21.2 GiB of 24 GiB); launch waits for headroom per docs/compute.md. Qwen arms are out-of-scope-pending if the supervisor reasserts the 8B ceiling (ADR 0015). Generation completed overnight 2026-08-21/22 via the rearmed day-2 waiter; evaluated at commit 0068842 (no pipeline code changed since generation at 6fc4bba). Reading: on Qwen3.5-9B, oracle guidance materially and significantly improves decomposition quality across the board (step_f1, ordered_step_accuracy, rouge_l_f1, exact_match, hop_count_EM all p<0.05 by bootstrap CI, McNemar, and paired t-test) at +13% tokens/+26% latency — a broader signal than the same comparison on Mistral-7B (exp-004). unguided_capped is statistically indistinguishable from unguided here too (cap fired on only 4/600 rows). No router-design recommendation made; that decision is Jahid's/the supervisor's. Committed artifacts: `experiments/exp-005/{config.json,metrics.json,notes.md}`. |
| exp-006 | 2026-08-20 | experiment-runner:session-1 | Issue #14 / ADR 0021: the clustering pool-construction strategy at size 2000 should end up sitting next to the existing `imbalanced` and `balanced` strategies at the same size, same dev sample, same retrieval variants — so the sweep's decomposition metrics are a construction-vs-construction comparison, not a confound of size or eval set. Verified in this log before starting: no v2 `pool_sweep` cell (any of the three strategies) has ever produced metrics — the sweep has only ever dry-run in v2 (PR #34/#35/#36 added the clustered strategy, code and significance tooling, but no `pool_sweep` run landed here). Minimal set to answer #14: `(size=2000, balance) x (variant, mode)` for `balance in {imbalanced, balanced, clustered}`, `variant in {biencoder_only, biencoder_plus_ce}`, `mode in {raw, typed, uniform}` — 3 x 2 x 3 = 18 decompose+eval cells, one trial each (`pool_trial_seeds: [42]`), all against one dev sample (`dev_seed 42`, `dev_per_hop 250`, 3 coarse buckets = 750 queries). This run covers only the CPU-feasible half of that plan (pool construction + bi-encoder similarity + cross-encoder rerank, all 3 strategies); the GPU half (mistral_7b_instruct decomposer generation x18 + eval) is queued behind exp-004/exp-005's hold on `runs/run.lock`. | `configs/pool_sweep.json` (`clustered_sizes: [2000]`, `imbalanced_sizes`/`balanced_sizes` include 2000; `retrieval_modes`, `retriever_variants`, `dev_seed`, `dev_per_hop`, `base_pool_seed`/`pool_trial_seeds`), `configs/musique_prep.json` `sample_pool.clustering` (ADR 0021 knobs), `configs/similarity.json`. Ran `MusiQue/scripts/sample_dev.py`, `sample_pool.py` (x3 balances), `check_question_similarity.py`, `truncate_top20.py`, `rerank_similarity_results.py` directly (not through `scripts/pool_sweep_orchestrator.py`) with every value the orchestrator would pass left at the child script's own committed default (`--config musique_prep.json` / `similarity.json`, `--embed-model e5-small`, `--top-k 20`, `--k 5`/`--rerank-k 5`, `--cross-encoder cross-encoder/ms-marco-MiniLM-L-12-v2`) and `--device cpu` explicit — this is a deliberate deviation from `pool_sweep.json`'s own top-level `"device": "cuda"`, which the orchestrator forces onto these same stages unconditionally (confirmed live via `--dry-run`: it prints `--device cuda` for `similarity`/`rerank` on every cell and for `sample_pool` on the clustered cell). CPU is not an invented value: `configs/similarity.json`'s own `"device"` key already defaults to `"cpu"`, and ADR 0021 item 7 records that this repo's only *verified* bit-reproducibility check for the clustered strategy's k-means fit is CPU-only (GPU embedding determinism is explicitly unverified there) — so CPU is the more, not less, defensible device for this precompute. Every output was written to the exact path `pool_sweep_orchestrator.py`'s own `_pool_dir`/`_sim_dir`/`_bi_top5_dir`/`_ce_dir`/`_dev_sample_path` helpers produce (verified against the orchestrator's own `--dry-run` printout for `--only size2000_imbalanced,size2000_balanced,size2000_clustered`), so a later `--stage decompose` (or `--stage all`) invocation skips this precompute via its existing skip-if-exists logic and proceeds straight to the GPU stage. | b0a9ce8 | 42 | none in this run (embed `intfloat/e5-small-v2` via `e5-small` alias; cross-encoder `cross-encoder/ms-marco-MiniLM-L-12-v2`; decomposer `mistralai/Mistral-7B-Instruct-v0.3` (`mistral_7b_instruct`) queued for the deferred GPU stage) | pool_sweep's own dev sample (`dev_seed 42`, `dev_per_hop 250`, coarse buckets `2hop/3hop/4hop` = 750 queries) — distinct from ADR 0007's pinned 600 (200/hop) used by exp-002..005; decomposition-quality metrics are still PENDING, deferred to the GPU follow-up | CPU precompute COMPLETE (all 3 strategies, rc=0 every stage, ~13.7 min wall clock 21:09:17→21:23:00 local): pool.jsonl 2000/2000/2000 rows (hop split imbalanced 1461/413/126, balanced 667/667/666, clustered 1396/495/109); top20/top5-biencoder/top5-ce all 750/750/750 rows per strategy. Clustered k-means: 19,938 candidates→k=2000, 11 iters, inertia 1803.46, 0 empty clusters, 0 rows from top-up, embed 33.4s + fit 25.4s (CPU, single-threaded per ADR 0021). Decomposer generation + eval (18 cells: 3 balances x 2 variants x 3 modes) still NOT RUN — queued behind exp-004/exp-005's `runs/run.lock` hold. Full metrics: `experiments/exp-006/metrics.json`. | Ran detached in tmux session `exp-006` (session ended on completion), log `runs/exp-006/cpu_stages.log`, launcher `runs/exp-006/run_cpu_stages.sh`. Never touched `runs/run.lock`, the exp-004/005 waiter, or its tmux session — confirmed by `nvidia-smi` identical before and after (21279 MiB used / 2972 MiB free both times). Committed artifacts: `experiments/exp-006/{config.json,metrics.json,notes.md}`; precompute outputs stay on the box under `runs/pool_sweep/{dev_sample,pools,similarity,biencoder_top5,rerank}/`. Flag for the GPU follow-up: this dev sample carries no `question_masked_typed`/`question_masked_uniform` fields (`sample_dev.py` reported `typed=False, uniform=False`); whether that degrades the `typed`/`uniform` retrieval modes was not checked here. Follow-up command once the lock frees: `pool_sweep_orchestrator.py --stage decompose --only size2000_imbalanced,size2000_balanced,size2000_clustered` then `--stage eval` (or one `--stage all` pass) — it will skip this run's precompute via skip-if-exists and go straight to the GPU stage. |
| exp-007 | 2026-08-21 | experiment-runner:session-1 | Issue #15 / ADR 0022: whether hop-matched few-shot retrieval helps decomposition quality is unmeasured. This run produces the CPU-side retrieval artifacts for two of the three conditions (mixed, oracle-hop-matched) on the same eval set and same pool, so a later GPU decomposition + eval pass turns the gap into a number instead of an assumption. Decomposition-quality metrics are explicitly deferred; this entry covers artifact production only. The router-hop-matched condition is NOT runnable (issue #15 comment, ADR 0022 consequences: today's router writes no query id) and is not attempted here. | `MusiQue/scripts/check_question_similarity.py` -> `truncate_top20.py` -> `rerank_similarity_results.py`, run directly (not via an orchestrator) for `mixed` (`--no-hop-match`) and `oracle` (`--hop-match --hop-source gold`), both `--mode typed` (ADR 0006 item 4 default; masking default itself remains supervisor-open per ADR 0018 — #15's conditions are stated at the typed default, not a resolution of that question), `--top-k 20`, rerank `--rerank-k 5`, truncate `--k 5`, `--device cpu`, `--seed 42`, `configs/similarity.json` defaults otherwise (`e5-small` bi-encoder, `cross-encoder/ms-marco-MiniLM-L-12-v2` reranker). Queries: `--query-glob musique/dev_data/musique_ans_v1.0_dev_sample_*_hop_200.jsonl --n 200` = the ADR 0007 pinned 600 (200/hop, verified by dry-run preflight below). Pool: `--pool-file` pointed at the concrete file located and hash-verified against ADR 0014, `musique/chunks_only_question_masked_fixed/roberta_large_ner_english/musique_ans_v1.0_train_0_questions_all.jsonl` under the data root. The two conditions differ only in the hop_match flag; every other flag is identical. | d81dd5b | 42 | none (retrieval only: `intfloat/e5-small-v2` bi-encoder, `cross-encoder/ms-marco-MiniLM-L-12-v2` reranker; decomposer deferred to the GPU follow-up) | MuSiQue ADR 0007 pinned 600 (200/hop) | COMPLETE — CPU precompute only, both conditions rc=0. Row counts (re-verified independently at completion, not just from the per-stage trail): `mixed` and `oracle` each 600/600 at similarity_top20/truncate_top5/rerank_top5; hop split by query_id prefix on both final rerank_top5 files = 200/200/200. `oracle`'s hop_match provenance: hop_source=gold, min_candidates_resolved=20, per-hop pool candidates 3594/4387/1175 (hop 2/3/4) — every bucket clears min_candidates=top_k=20. `mixed`'s hop_match provenance: enabled=false on every query file (byte-identical code path, ADR 0022 item 2). Final artifact sha256 for all 6 files recorded in `experiments/exp-007/metrics.json`. Decomposition-quality metrics remain UNMEASURED here by design — deferred to exp-009. | Preflight (`--dry-run`, CPU, no encoder loaded) run for both conditions before launch: 600/600 pinned query ids loaded across the 3 glob-matched files, 200/200/200 per hop. Pool file located by matching its sha256 against ADR 0014's pinned hash: `sha256sum` of `musique_ans_v1.0_train_0_questions_all.jsonl` = `212c27634291b27ef55cebdb5feaba08a32a59f21976dd72a96a8f9f1ae66b2a`, exact match; 9,156 rows; observed hop split via query id prefix = 2-hop 3,594 / 3-hop 4,387 / 4-hop 1,175 — matches ADR 0022's cited bucket counts exactly. Oracle dry-run's per-bucket candidate counts (3594/4387/1175, all >= the default `min_candidates = top_k = 20`) confirm every hop bucket is feasible at top-k 20 before spending any CPU time on embeddings. GPU untouched: `runs/run.lock` (held `exp-004,exp-005` since 2026-08-19T15:03:03+03:00) and the exp-004/005 waiter were not touched; `nvidia-smi` shows 21279 MiB used / 2972 MiB free both before and after, attributable to another user's `ollama` process (pid confirmed via `nvidia-smi --query-compute-apps`), same as exp-006's note. Working tree carries one untracked non-pipeline file (`Plan after the meeting.md`, present at session start, not created by this run) — flagged by the run's own dirty-tree warning; does not affect any config or code path. **Completion housekeeping (2026-08-22, experiment-runner:session-1, no new run):** this row's metrics cell was left reading "PENDING — run launching" after the 6fc4bba commit even though the artifacts (`runs/exp-007/{mixed,oracle}/`, timestamps 01:02–01:05) finished minutes later; `experiments/exp-007/{config.json,metrics.json,notes.md}` were never committed (empty dir). Both are filled in now from the already-completed on-disk artifacts and their per-stage trail (`runs/exp-007/**/{similarity,truncate,rerank}/*_metrics.json`), re-verified independently (row counts, hop split, sha256) rather than copied blind. This is the required "completed after" edit to an existing row, not a new experiment; the GPU decomposition + eval follow-up is logged separately as exp-009. |
| exp-008 | 2026-08-22 | experiment-runner:session-1 | Issue #13: whether LoRA fine-tuning on the full MuSiQue train split beats the few-shot dynamic-prompting method on the same pinned eval set, and at what per-query cost, is unmeasured. This run produces the fine-tuned arm's decompositions and metrics on the ADR 0007 pinned 600 (inference + eval only; no training — `full_train`'s adapter is already trained, exp-001), so the comparison against exp-004's Mistral prompting arms becomes numbers. | `components/decomposer/run_decomposer.py --model mistral_7b_instruct --config decomposer_musique.json --condition unguided --adapter runs/exp-001/20260818_222259/adapter --no-few-shot --output-root runs/exp-008` (condition `unguided`'s prompt — guided false, no hop line, no few-shot block — matches the `full_train` training record exactly per `check_adapter_prompt_parity`, which the dry run below reports 0 mismatches on); eval via `scripts/musique_decompositions_evaluator.py`, then `scripts/compare_decomposer_arms.py --eval-arm full_train` against exp-004's mistral prompting arms (`configs/finetune_decomposer.json` arm `full_train`, `configs/musique_eval.json`) | ad252b4 | 42 | mistralai/Mistral-7B-Instruct-v0.3 + LoRA adapter (QLoRA r=16, trained exp-001 `full_train`, 2026-08-19; base+adapter within the 10B ceiling per ADR 0015) | MuSiQue ADR 0007 pinned 600 (200/hop), retrieval artifact per ADR 0014 (v1 9,156-example pool, sha256 e5c418a9b25f...; retrieval file supplies the query set even though `--no-few-shot` empties the example block) | COMPLETE — rc=0, 600/600 rows, 0 rows at max_new_tokens. Overall: composite 0.5236, step_f1 0.3411, ordered_step_acc 0.3237, hop_count_EM 0.7100, exact_match 0.1083, step_count_MAE 0.300, mean_signed_error -0.233 (under-decomposes, unlike all 3 prompting arms which over-decompose), over_rate 0.033, under_rate 0.257. Per-hop (step_f1/hop_count_EM/over/under/MAE/signed) hop=2: 0.4810/0.9750/0.020/0.005/0.025/+0.015; hop=3: 0.2930/0.7450/0.040/0.215/0.255/-0.175; hop=4: 0.2494/0.4100/0.040/0.550/0.620/-0.540. Cost/query: 122.5 prompt + 29.4 completion = 151.9 tok, 1.387s latency (vs unguided baseline's 499.8tok/1.879s — ~30% of tokens, ~74% of latency). Phase C `compare_decomposer_arms.py --eval-arm full_train --baseline unguided` (n=600 aligned, 200/200/200 per hop enforced on all 4 arms): full_train vs unguided — step_f1 +0.1372 CI[+0.1150,+0.1601] sig (t p=4.0e-29); ordered_step_acc +0.1433 CI[+0.1205,+0.1666] sig (t p=2.4e-31); rouge_l_f1 +0.1218 CI[+0.1065,+0.1369] sig (t p=7.7e-48); composite +0.3137 CI[+0.1055,+0.3307] sig; exact_match McNemar +0.0467 p=1.75e-04 sig (t p=1.29e-04); hop_count_EM McNemar +0.2017 p=1.12e-15 sig (t p=5.5e-16) — every quality metric significant in the fine-tuned arm's favour over the deployable prompting baseline, at lower cost. oracle_guided vs unguided and unguided_capped vs unguided reproduce exp-004's within-model numbers (included for context, not re-derived). full_train also exceeds oracle_guided on step_f1 (0.3411 vs 0.2061) and ordered_step_acc (0.3237 vs 0.1852) even though oracle_guided is not a deployable arm. Full tables: `experiments/exp-008/metrics.json`. | Dry-run preflight before launch: 600/600 pinned ids, 200/200/200 per hop, retrieval sha256 matches ADR 0014, `adapter_prompt_parity.mismatches == []` against the training record at `runs/exp-001/20260818_222259/config.json` (trained_on guided=false/prompt_unguided.md/plain/few_shot_examples_in_prompt=false; run renders the same), `assert_adapter_base_model` passes (both `mistralai/Mistral-7B-Instruct-v0.3`). `pool_2000` arm remains BLOCKED on Jahid's best-pool decision (issue #13); `generalisation_2_3hop` not trained; neither is touched here. `runs/run.lock` transferred from `exp-004,exp-005` (their GPU work is done; Phase A's evaluation was CPU-only) to `exp-008` for this run, then released after this entry and its artifacts were committed. Launched in tmux (`runs/exp-008/run.log`), GPU free at launch and throughout (no contention this time). Baseline for Phase C is `unguided` (the deployable dynamic-prompting condition) per the brief; `configs/finetune_decomposer.json` does not designate a different baseline protocol. `pool_2000`/`generalisation_2_3hop` remain unmeasured — this entry covers `full_train` only. Committed artifacts: `experiments/exp-008/{config.json,metrics.json,notes.md}`. |
| exp-009 | 2026-08-22 | experiment-runner:session-1 | Issue #15 / ADR 0022 GPU follow-up: whether hop-matched few-shot retrieval (oracle-hop-matched) improves decomposition quality over mixed retrieval, at the `unguided` (deployable) prompting condition, on the ADR 0007 pinned 600. Router-hop-matched is out of scope (no query id from today's router, ADR 0022 item 5/Consequences) — not attempted, not stubbed, not estimated. | `components/decomposer/run_decomposer.py --model mistral_7b_instruct --config decomposer_musique.json --condition unguided --retrieval-input runs/exp-007/<mixed\|oracle>/<cond>_rerank_top5.jsonl --output-root runs/exp-009/<mixed\|oracle>`, everything else (seed, model, decoding, `--retrieval-mode`/`--retrieval-k` left at config defaults `typed`/5, matching the ADR 0006 masking default the exp-007 artifacts were built at) identical between the two arms; retrieval-source wiring per PR #39 (d81dd5b) — `--retrieval-input` overrides `retrieval.input`/`retrieval.input_key` with no other code path touched (frozen launch-path files unchanged by PR #39). Eval via `scripts/musique_decompositions_evaluator.py` (per-hop reporting incl. over/under-decomposition rates) then `--compare mixed vs oracle_hop_matched` (bootstrap CIs + McNemar + paired t-test). | 133c45f | 42 | mistralai/Mistral-7B-Instruct-v0.3 (issue #12) | MuSiQue ADR 0007 pinned 600 (200/hop), retrieval artifacts `runs/exp-007/mixed/mixed_rerank_top5.jsonl` and `runs/exp-007/oracle/oracle_rerank_top5.jsonl` (exp-007, sha256 recorded in `experiments/exp-007/metrics.json`) | COMPLETE — both arms rc=0, 600/600 rows, 0 empty decompositions (mixed 23/600 rows at max_new_tokens, oracle 32/600). NOTE (issue #40, `docs/analysis/2026-08-22-finetuned-vs-prompting-error-analysis.md`, landed mid-run at `ec2f844`, docs-only): composite_score's 0.2-weight reference_validity term is decided by a regex/bare-vs-bracketed-`#k` mismatch — recorded per schema but NOT headlined; headline is step_f1 / ordered_step_accuracy / hop_count_exact_match. Overall (mixed / oracle_hop_matched): step_f1_macro 0.2035 / 0.1931; ordered_step_accuracy_macro 0.1804 / 0.1799; hop_count_exact_match_rate 0.5150 / 0.6000; exact_match_rate 0.0617 / 0.0567; composite_score 0.2099 / 0.2633 (suspect, see note); reference_validity_micro 0.0 / 0.2778; over/under-decomposition 0.285/0.200 vs 0.270/0.130; step_count_MAE 0.77 / 0.703. Per-hop (step_f1/ordered_step_acc/hop_count_EM) hop=2/3/4: mixed 0.327/0.318/0.710, 0.168/0.138/0.525, 0.116/0.085/0.310; oracle 0.355/0.345/0.845, 0.153/0.135/0.580, 0.071/0.060/0.375 — oracle's hop-count gain concentrates at hop 2 and 3/4 hold smaller gains, but oracle's step_f1/ordered_step_acc are behind mixed at hop 4 (the smallest pool bucket, 1,175/9,156 rows — ADR 0022's stated confound). --compare (n=600, bootstrap 10000 resamples seed 42 + McNemar + paired t-test): step_f1 diff +0.0104 CI[-0.0064,+0.0273] not sig (t p=0.226); ordered_step_accuracy +0.0005 CI[-0.0157,+0.0168] not sig (t p=0.950); rouge_l_f1 -0.0038 CI[-0.0159,+0.0079] not sig (t p=0.530); composite_score -0.0534 CI[-0.1974,+0.1856] not sig by bootstrap (suspect metric regardless); exact_match McNemar p=0.664 not sig (t p=0.513); hop_count_exact_match McNemar p=0.0006164 (b=82,c=133) SIG, t p=0.00048 SIG (mixed 0.515 vs oracle 0.600, oracle ahead). Reading: hop-matched retrieval moves whether the model gets the step COUNT right significantly, with no measured step-QUALITY gain (step_f1/ordered_step_accuracy both not significant) — the same shape as exp-004's guided/unguided finding, on an unrelated axis (few-shot retrieval vs stated hop count). Full tables: `experiments/exp-009/metrics.json`. Router-hop-matched remains unmeasurable; the oracle-minus-router error-propagation number is NOT estimated anywhere in this entry. | Preflight before launch (both conditions, `--dry-run`): confirmed 600/600 pinned ids load from each retrieval file and 200/200/200 per hop, 0 ids_missing/ids_unexpected, retrieval input sha256 matched exp-007's artifacts exactly. `runs/run.lock` acquired for `exp-009`; GPU verified idle at acquisition (`nvidia-smi`: 10 MiB used / 24242 MiB free, no compute processes). Launched detached in tmux (`runs/exp-009/run_exp009.sh`, log `runs/exp-009/run.log`); completed with tmux session ended, `overall_rc=0`. Generation ran from the process image loaded at commit `133c45f`; `main` advanced to `ec2f844` (docs-only, issue #40's analysis note, verified to touch no pipeline code) while the GPU run was in flight, so the runner's own trail records `git.commit: ec2f844` (it reads HEAD at write time) even though no code relevant to this run changed between the two commits — see `experiments/exp-009/config.json`'s `note_on_commit_delta`. `runs/run.lock` released after this entry and its artifacts were committed. Committed artifacts: `experiments/exp-009/{config.json,metrics.json,notes.md}`. |
| exp-010 | 2026-08-22 | experiment-runner:session-1 | Issue #14 / ADR 0021 GPU follow-up: whether the clustering pool-construction strategy (size 2000) matches, beats, or lags `imbalanced`/`balanced` on decomposition quality, at the same size, same 750-query dev sample, same two retriever variants and three retrieval modes, is unmeasured. exp-006 produced the CPU precompute (pool construction + bi-encoder similarity + CE rerank) for all 3 strategies; this run is the deferred GPU half — 18 decompose+eval cells (3 strategies x 2 retriever variants x 3 retrieval modes) — that actually produces the comparison issue #14 asks for. PRE-LAUNCH CHECK (required by the brief) verified with evidence before any cell was launched: the dev sample lacks `question_masked_typed`/`question_masked_uniform` fields (exp-006's own flag), but `check_question_similarity.py`'s own docstring says it NER-masks queries on the fly when those fields are missing; read directly off exp-006's committed `top20.jsonl` artifacts (all 3 balances), `typed_top_k`/`uniform_top_k` candidate-id lists differ from `raw_top_k` on **750/750 rows in every one of the 3 pool balances** (0 rows identical across all three modes) — so `typed`/`uniform` are genuinely distinct retrieval conditions here, not a silent fallback to raw, and all 18 cells are meaningful (none cut). Issue #14's own 2026-08-2x comment independently records the same finding. Also checked: `configs/pool_sweep.json` points `configs.decomposer` at the MetaQA-flavoured `configs/decomposer.json` (hops [1,2,3], `questions_template_key` metaqa) rather than `decomposer_musique.json`; traced in `run_decomposer.py` that with `--retrieval-input` supplied (which the orchestrator always passes for this sweep) the question-loading branch that uses `hops`/`questions_template_key` is never reached — hop comes from parsing the retrieval file's own `query_id` prefix — and the pinned-eval-set assertion is a no-op because `decomposer.json` declares no `eval_rows_per_hop` (verified in the runner's own source: an absent key short-circuits the check with `"this config declares no 'eval_rows_per_hop', so no pinned-set assertion applies"`). No `conditions` block in `decomposer.json` means the run behaves as the plain `guided: false` unguided arm (`resolve_condition`'s documented behaviour for a config without one). Not a blocker; recorded so the next reader does not mistake `decomposer.json` for a MuSiQue-shaped config by name alone. | `scripts/pool_sweep_orchestrator.py --stage decompose --only size2000_imbalanced,size2000_balanced,size2000_clustered` then `--stage eval` (same `--only`), against `configs/pool_sweep.json` (`decomposer_model_folder: mistral_7b_instruct`, `decomposer_quantization: 4bit`, `retrieval_k: 5`) unedited; `--dry-run` preflight run for both stages first (confirmed: dev sample and all CPU precompute skip via skip-if-exists — no similarity/pool/rerank command is even printed for `--stage decompose`/`--stage eval`, only the 18 decomposer/evaluator invocations — GPU/CPU precompute is never re-touched). Headline comparison after both stages complete: `scripts/musique_decompositions_evaluator.py --compare <a>_per_item.json <b>_per_item.json` pairwise across `imbalanced`/`balanced`/`clustered` at `biencoder_plus_ce` + `typed` (ADR 0006 item 4's stated default, applicable here since the pre-launch check confirms `typed` is genuinely distinct from `raw` for this dev sample). | 6831ee5 | 42 | mistralai/Mistral-7B-Instruct-v0.3 (`mistral_7b_instruct`, 4-bit; issue #12) | pool_sweep's own dev sample (`dev_seed 42`, `dev_per_hop 250`, coarse buckets 2hop/3hop/4hop = 750 queries; distinct from the ADR 0007 pinned 600 used by exp-002..005/008/009) | COMPLETE — all 18 decompose+eval cells rc=0, 750/750 rows evaluated on every cell, 0 missing gold. Headline cell (`biencoder_plus_ce` + `typed`, ADR 0006 item 4 default) overall step_f1/ordered_step_accuracy/hop_count_exact_match/composite (composite recorded, NOT headlined — issue #40): imbalanced 0.1900/0.1706/0.5133/0.2001; balanced 0.1926/0.1710/0.4667/0.1974; clustered 0.1939/0.1775/0.5133/0.2038. Pairwise `--compare` (bootstrap 95% CI + McNemar + paired t-test, seed 42, n=750 aligned): step_f1/ordered_step_accuracy/rouge_l_f1/exact_match NOT significant in any of the 3 pairs (all CIs cross 0). hop_count_exact_match_rate is the one significant axis: imbalanced vs balanced McNemar p=0.0438 (t p=0.0381), balanced vs clustered McNemar p=0.0380 (t p=0.0328) — imbalanced (0.5133) and clustered (0.5133, exactly tied) both significantly beat balanced (0.4667); imbalanced vs clustered indistinguishable (identical rate, McNemar p=1.0). Per-hop (step_f1/ordered_step_accuracy/hop_count_EM) hop=2: imbalanced 0.302/0.290/0.756, balanced 0.282/0.268/0.584, clustered 0.320/0.310/0.716 — the hop-count gap concentrates here; hop=3: imbalanced 0.151/0.127/0.472, balanced 0.174/0.152/0.504, clustered 0.152/0.129/0.552 — clustered leads hop-count, step metrics flat; hop=4: imbalanced 0.117/0.095/0.312, balanced 0.122/0.093/0.312, clustered 0.110/0.093/0.272 — all converge (smallest pool bucket in every strategy). Reading: pool-construction strategy moves whether the model states the right step COUNT, not whether the steps it emits are the right ones — same shape as exp-005/exp-009 on unrelated axes. No claim that clustering beats imbalanced: statistically indistinguishable here; both beat balanced only on hop-count. Full 18-cell table, per-hop breakdown and raw `--compare` JSONs: `experiments/exp-010/metrics.json`. | Rc verified independently off every eval_metrics.json (not just the orchestrator trail): 18/18 decompose rc=0 (~1515–1792s/cell, ~8h10m total), 18/18 eval rc=0 (~5–9s/cell); wall clock 2026-08-22T11:23:00Z (lock acquired) → 19:33:08Z (run.log). SHARED-CHECKOUT COMPLICATION, resolved with evidence (see `experiments/exp-010/config.json`): this run's 8h10m span crossed three self-merged PRs (`fe4b32f` #43 router readiness, `a940376` #42 end-to-end backends, `a99b573` #44 additive Break-faithful metrics) plus 2 docs-only commits; each of the orchestrator's 36 subprocess calls is spawned fresh and reads current disk state, so imbalanced ran entirely pre-merge/docs-only, balanced split across docs-only and `a940376`, clustered ran entirely on `a99b573`, and all 18 eval cells (one contiguous burst after 19:31 UTC) carry `a99b573`'s new additive columns. Verified via targeted `git diff` that none of this touches a value this row reports: `configs/pool_sweep.json`/`similarity.json`/`musique_prep.json`/the mistral model config/prompt files are byte-identical across the whole range; `run_decomposer.py`'s only change (`resolve_hop_source`, #43) is gated on `guided=true` or an explicit `hop_source` condition key — neither set by this sweep's unguided config — so it is a documented no-op; the evaluator's #44 change is additive by its own docstring and `_statistics_available()`'s missing-column handling, with `composite_score_weights` byte-identical in `configs/musique_eval.json`. Per the coordinator's explicit instruction this is NOT a re-evaluation under the new metrics (the 4 new columns are recorded per schema in `eval_metrics.json` but unused in this row's comparison) — the nine-arm re-score under the new metrics is a separate, later assignment. `runs/run.lock` released after this entry and its artifacts were committed. Committed artifacts: `experiments/exp-010/{config.json,metrics.json,notes.md}`. |
